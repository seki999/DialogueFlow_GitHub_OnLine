speaker 1: 这一章叫“表单绑定、验证与 PRG”。它要帮我们解决什么问题？
speaker 2: 掌握新增编辑页面从显示到校验再到重定向的完整闭环。
speaker 1: 图从“GET 表单”开始，我应该先抓住哪条主线？
speaker 2: 沿着箭头依次看：GET 表单、Form DTO、POST 提交、Jakarta Validation、BindingResult、Redirect。这就是本章的因果或调用顺序。
speaker 1: 第一项核心信息为什么重要？
speaker 2: GET 请求准备 Form DTO，Thymeleaf 用 th:field 绑定输入控件。
speaker 1: 那第二个要点补充了什么？
speaker 2: POST 使用 @Valid 触发 Jakarta Validation，BindingResult 保存错误。
speaker 1: 组件职责最容易在哪里混淆？
speaker 2: 存在验证错误时返回原表单并显示字段级提示。
speaker 1: 如果把它放进实际运行流程，还要注意什么？
speaker 2: 成功保存后采用 Post/Redirect/Get，避免刷新导致重复提交。
speaker 1: 有没有一个必须明确写出来的现实边界？
speaker 2: Service 只把 Form DTO 允许的字段复制到 Entity，控制可修改范围。
speaker 1: 所以不能只看到图上有节点，就断言所有能力都已经生产化，对吗？
speaker 2: 对。客户端校验不能替代服务端 Jakarta Validation。 同时，BindingResult 必须紧跟被验证的表单参数。
speaker 1: 我来复述一下：先按图理解顺序，再区分职责，最后核对实现边界。
speaker 2: 这个复述很准确。课程始终把已实现内容、模拟内容和生产建议分开。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道单项选择题，检查自己是否真正理解了这些内容。
