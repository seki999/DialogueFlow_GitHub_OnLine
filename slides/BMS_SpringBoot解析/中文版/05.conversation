speaker 1: 这一章叫“Fragment 与公共页面骨架”。它要帮我们解决什么问题？
speaker 2: 理解公共布局如何减少导航、页头和脚本的重复。
speaker 1: 图从“公共 Fragment”开始，我应该先抓住哪条主线？
speaker 2: 沿着箭头依次看：公共 Fragment、页面参数、内容片段、布局组合、一致页面。这就是本章的因果或调用顺序。
speaker 1: 第一项核心信息为什么重要？
speaker 2: Thymeleaf Fragment 可复用页头、导航、消息区域和脚本引用。
speaker 1: 那第二个要点补充了什么？
speaker 2: 业务页面只提供当前页面主体与必要参数。
speaker 1: 组件职责最容易在哪里混淆？
speaker 2: 公共布局让权限导航和样式入口集中维护。
speaker 1: 如果把它放进实际运行流程，还要注意什么？
speaker 2: Fragment 是服务端模板复用，不是独立前端组件运行时。
speaker 1: 有没有一个必须明确写出来的现实边界？
speaker 2: 修改公共骨架会影响所有引用它的模板，因此需要回归页面。
speaker 1: 所以不能只看到图上有节点，就断言所有能力都已经生产化，对吗？
speaker 2: 对。Fragment 能复用标记，但不等同于 Vue 的响应式组件。 同时，公共布局不应偷偷执行业务数据库查询。
speaker 1: 我来复述一下：先按图理解顺序，再区分职责，最后核对实现边界。
speaker 2: 这个复述很准确。课程始终把已实现内容、模拟内容和生产建议分开。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道单项选择题，检查自己是否真正理解了这些内容。
