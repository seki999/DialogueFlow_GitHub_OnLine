speaker 1: 这一章叫“Actuator、指标与优雅停机”。它要帮我们解决什么问题？
speaker 2: 理解进程健康、流量就绪、指标采集和终止阶段的不同信号。
speaker 1: 图从“应用运行”开始，我应该先抓住哪条主线？
speaker 2: 沿着箭头依次看：应用运行、Liveness、Readiness、Micrometer、Prometheus、Graceful Shutdown。这就是本章的因果或调用顺序。
speaker 1: 第一项核心信息为什么重要？
speaker 2: Actuator 暴露 health、info、metrics 和 prometheus 等管理端点。
speaker 1: 那第二个要点补充了什么？
speaker 2: Liveness 用于判断进程是否需要重启。
speaker 1: 组件职责最容易在哪里混淆？
speaker 2: Readiness 用于判断实例是否可以接收流量。
speaker 1: 如果把它放进实际运行流程，还要注意什么？
speaker 2: Micrometer Prometheus Registry 输出 JVM、HTTP 和应用指标。
speaker 1: 有没有一个必须明确写出来的现实边界？
speaker 2: Graceful Shutdown 在终止时尽量完成已有请求。
speaker 1: 所以不能只看到图上有节点，就断言所有能力都已经生产化，对吗？
speaker 2: 对。健康端点通过不等于所有外部设备和业务流程都正常。 同时，生产中应控制管理端点网络暴露和敏感信息。
speaker 1: 我来复述一下：先按图理解顺序，再区分职责，最后核对实现边界。
speaker 2: 这个复述很准确。课程始终把已实现内容、模拟内容和生产建议分开。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道单项选择题，检查自己是否真正理解了这些内容。
