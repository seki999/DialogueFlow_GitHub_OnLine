speaker 1: 这一章叫“Dashboard 组装与演示趋势边界”。它要帮我们解决什么问题？
speaker 2: 看懂仪表盘数据如何汇总，并识别演示趋势与生产统计的区别。
speaker 1: 图从“DashboardController”开始，我应该先抓住哪条主线？
speaker 2: 沿着箭头依次看：DashboardController、DashboardService、多仓储统计、DashboardView、模板与 Canvas。这就是本章的因果或调用顺序。
speaker 1: 第一项核心信息为什么重要？
speaker 2: DashboardController 调用 DashboardService.getDashboard 并把 DashboardView 放入 Model。
speaker 1: 那第二个要点补充了什么？
speaker 2: Service 汇总设备、事件、告警和 TCP Ping 数据。
speaker 1: 组件职责最容易在哪里混淆？
speaker 2: 模板展示统计卡片，少量原生 JavaScript 或 Canvas 绘制趋势。
speaker 1: 如果把它放进实际运行流程，还要注意什么？
speaker 2: 当前近 24 小时趋势由事件总量和稳定权重生成，是学习演示数据。
speaker 1: 有没有一个必须明确写出来的现实边界？
speaker 2: 生产统计应按 occurred_at 做真实的逐小时数据库聚合。
speaker 1: 所以不能只看到图上有节点，就断言所有能力都已经生产化，对吗？
speaker 2: 对。演示趋势不能用于生产容量或故障分析结论。 同时，Canvas 只负责展示，不应被误解为统计数据来源。
speaker 1: 我来复述一下：先按图理解顺序，再区分职责，最后核对实现边界。
speaker 2: 这个复述很准确。课程始终把已实现内容、模拟内容和生产建议分开。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道单项选择题，检查自己是否真正理解了这些内容。
