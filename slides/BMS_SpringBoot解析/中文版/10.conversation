speaker 1: 这一章叫“认证、角色权限与 CSRF”。它要帮我们解决什么问题？
speaker 2: 建立从登录到 URL、方法和表单三层保护的安全模型。
speaker 1: 图从“登录认证”开始，我应该先抓住哪条主线？
speaker 2: 沿着箭头依次看：登录认证、SecurityFilterChain、URL 权限、PreAuthorize、CSRF 校验、受控操作。这就是本章的因果或调用顺序。
speaker 1: 第一项核心信息为什么重要？
speaker 2: Spring Security 6 通过 SecurityFilterChain 配置请求安全。
speaker 1: 那第二个要点补充了什么？
speaker 2: 本地学习实现使用 ADMIN、OPERATOR、VIEWER 三类内存账号。
speaker 1: 组件职责最容易在哪里混淆？
speaker 2: 管理路径使用 URL 规则，并通过 @PreAuthorize 做方法级二次保护。
speaker 1: 如果把它放进实际运行流程，还要注意什么？
speaker 2: 表单 POST 带 CSRF Token，防止跨站请求伪造。
speaker 1: 有没有一个必须明确写出来的现实边界？
speaker 2: InMemoryUserDetailsManager 与 API Key 适合学习边界，生产应接企业身份与更强 API 安全。
speaker 1: 所以不能只看到图上有节点，就断言所有能力都已经生产化，对吗？
speaker 2: 对。@sec:authorize 控制页面可见性，不替代服务端授权。 同时，/api/** 忽略 CSRF 后仍需要独立、可靠的 API 认证与防护。
speaker 1: 我来复述一下：先按图理解顺序，再区分职责，最后核对实现边界。
speaker 2: 这个复述很准确。课程始终把已实现内容、模拟内容和生产建议分开。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道单项选择题，检查自己是否真正理解了这些内容。
