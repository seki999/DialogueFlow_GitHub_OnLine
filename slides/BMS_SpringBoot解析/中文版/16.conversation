speaker 1: 这一章叫“Syslog 接收、解析与失败保留”。它要帮我们解决什么问题？
speaker 2: 理解 UDP/TCP 接收器的生命周期、解析边界和错误数据去向。
speaker 1: 图从“UDP 或 TCP 5514”开始，我应该先抓住哪条主线？
speaker 2: 沿着箭头依次看：UDP 或 TCP 5514、SyslogReceiver、SyslogParser、RFC3164 或 RFC5424、IngestRequest、Event。这就是本章的因果或调用顺序。
speaker 1: 第一项核心信息为什么重要？
speaker 2: SyslogReceiver 是 Spring 管理的 SmartLifecycle 后台组件。
speaker 1: 那第二个要点补充了什么？
speaker 2: 默认同时监听 UDP 5514 与 TCP 5514，生产可映射标准端口 514。
speaker 1: 组件职责最容易在哪里混淆？
speaker 2: UDP 一 Datagram 一消息，TCP 按行读取并可交给虚拟线程处理连接。
speaker 1: 如果把它放进实际运行流程，还要注意什么？
speaker 2: SyslogParser 支持 RFC3164 与 RFC5424，并解析 PRI、Facility、Severity 等。
speaker 1: 有没有一个必须明确写出来的现实边界？
speaker 2: 解析失败会保留 rawMessage 并形成 syslog-parse-error Event，而不是静默丢弃。
speaker 1: 所以不能只看到图上有节点，就断言所有能力都已经生产化，对吗？
speaker 2: 对。UDP 没有应用层重传保证，慢数据库会带来接收风险。 同时，接收异常被隔离不代表可以忽略监控与容量规划。
speaker 1: 我来复述一下：先按图理解顺序，再区分职责，最后核对实现边界。
speaker 2: 这个复述很准确。课程始终把已实现内容、模拟内容和生产建议分开。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道单项选择题，检查自己是否真正理解了这些内容。
