speaker 1: 计算机怎么知道两句话意思相近，它又不像人一样能“读懂”文字？
speaker 2: 这就要靠 Embedding。简单说，就是把一段文字通过一个 Embedding 模型，转换成一串数字组成的向量，比如 [0.13, -0.24, 0.71]，这一串数字就代表了这段文字的语义。
speaker 1: 那两段文字意思相不相近，是怎么判断的？
speaker 2: 通过计算两个向量之间的余弦相似度，也就是 cosine similarity。如果两个向量在空间中的方向越接近，就说明这两段文字的语义越相似。
speaker 1: 所以本质上是把“语义相似”这个模糊的问题，转换成了一个可以用数学计算的问题？
speaker 2: 完全正确，这也是为什么向量检索能够找到那些字面上完全不同、但意思相近的内容，因为它比较的是语义空间里的距离，而不是关键词是否完全匹配。
speaker 1: 那是不是所有语言都用同一个 Embedding 模型就可以？
speaker 2: 不一定，不同语言可能需要用不同的 Embedding 模型才能获得更好的效果，比如支持多语言的 multilingual-e5、BGE、Jina Embeddings，还有 OpenAI Embeddings 和 Cohere Embed 这些商业化选项。
speaker 1: 如果企业客户既有中文文档又有英文文档，这时候该怎么选？
speaker 2: 这种情况下通常会优先考虑支持多语言的模型，比如 multilingual-e5 或者 BGE 的多语言版本，确保中英文内容都能被准确地映射到同一个语义空间里，这样跨语言检索也能正常工作。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
