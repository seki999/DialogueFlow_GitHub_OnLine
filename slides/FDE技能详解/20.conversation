speaker 1: 如果 Agent 判断应该给某个客户退款 50 万元，它可以直接自动执行这笔操作吗？
speaker 2: 不可以，这种高风险操作绝对不能让 Agent 直接自动执行。正确的做法是，Agent 先生成一份退款请求，然后交给人工审批，通过之后才真正调用支付接口去执行。
speaker 1: 除了退款，还有哪些操作类型属于这种需要人工审批的高风险类别？
speaker 2: 比较典型的包括删除数据、资金转账、生产环境部署、发送对外邮件、采购下单，以及修改生产环境配置，这些操作一旦出错，代价往往很高，甚至难以撤销。
speaker 1: 从工程实现的角度，怎么在 Agent 流程里加入这种人工审批环节？
speaker 2: LangGraph 提供了几个很关键的能力，比如 interrupt 可以在流程执行到某个节点时暂停，等待外部输入；checkpoint 可以保存当前的执行状态；resume 则可以在人工审批通过后，从暂停的地方继续执行。
speaker 1: 这几个能力组合起来，是不是就能实现“暂停等待审批，通过后继续执行”这样的完整流程？
speaker 2: 正是这样。Agent 执行到需要审批的节点时会触发 interrupt 暂停，同时用 checkpoint 保存当前状态，等人工在界面上点击“批准”之后，系统再用 resume 从暂停的地方恢复执行。
speaker 1: 所以 Human-in-the-loop 本质上是在自动化效率和风险控制之间找一个平衡点？
speaker 2: 完全正确，这也是为什么企业级 Agent 系统几乎都会设计审批节点，而不是把所有决定权完全交给 Agent 自动执行。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
