speaker 1: 如果一个 Agent 代表某个用户去查询一份公司内部文件，它应该用自己的身份去查，还是用这个用户的身份去查？
speaker 2: 这正是企业 Agent 架构里一个非常关键、也很容易被忽视的问题。用 Agent Identity，也就是 Agent 自己的身份去调用工具，实现起来更简单，但风险是 Agent 的权限范围如果设置得过大，可能导致用户能够越权访问本不该看到的数据。
speaker 1: 那如果改用 User Identity，也就是代表用户的身份去调用，会有什么不同？
speaker 2: 这样权限边界会更精确，用户能看到什么，Agent 代表他查询的时候也只能看到什么，但实现起来更复杂，需要引入委托授权这类机制，让 Agent 能够“代表”用户去访问系统，而不是冒用一个万能的超级权限身份。
speaker 1: 要支撑这种复杂的权限设计，通常需要掌握哪些相关的基础知识？
speaker 2: 至少需要理解 OAuth2、OIDC、SSO、SAML 这些身份认证协议，还有 RBAC 基于角色的访问控制、ABAC 基于属性的访问控制，以及 Secrets Management 密钥管理、KMS 密钥管理服务，这些共同构成了企业 IAM 体系。
speaker 1: 为什么最近 MCP 这个协议也特别在加强这方面的能力？
speaker 2: 因为 Agent 真正进入企业生产环境之后，“Agent 到底用什么身份去做事”已经变成了一个绕不开的核心问题，所以 MCP 目前专门在推进 enterprise-managed authorization 企业级授权管理，以及 agent identity 相关的能力。
speaker 1: 所以这已经不只是一个技术细节，而是关系到整个系统能不能被企业信任的关键设计？
speaker 2: 完全正确，如果这个问题处理不好，即使 Agent 的功能再强大，企业的安全团队也很难放心让它接入生产环境。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
