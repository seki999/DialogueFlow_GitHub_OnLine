speaker 1: 如果公司里财务部门的员工用这个 AI 系统提问，会不会有可能搜到 HR 部门那些不该看到的机密文件？
speaker 2: 如果系统设计不当，确实有可能，而这正是企业 RAG 项目里一个特别容易被忽视、但后果非常严重的问题，也就是权限感知检索。
speaker 1: 教程里好像很少提到这个，是不是因为技术上很难实现？
speaker 2: 技术实现本身并不复杂，只是很多教程只关注怎么把 RAG 跑起来，而没有考虑真实企业里“谁能看什么”这个问题。
speaker 1: 那具体应该怎么做？
speaker 2: 最基本的做法是，检索的时候不能只写 vector_search(query) 这样简单一句，而要带上过滤条件，比如根据用户所在的部门加上 filter，只在他有权限访问的数据范围内检索。
speaker 1: 能具体说说这个过滤是怎么实现的吗？
speaker 2: 比如财务部门的员工发起查询时，系统会自动加上 department 等于 finance 这样的过滤条件；HR 部门的员工则会加上 department 等于 hr 的过滤条件，这样向量检索的时候，系统只会在对应的数据范围里搜索。
speaker 1: 如果没有做这一层过滤，会有什么后果？
speaker 2: 后果可能非常严重，比如原本只能财务部门看到的薪酬数据，可能被其他部门的员工通过 AI 系统间接查到，这属于严重的数据泄露事故。
speaker 1: 所以这一层权限设计，其实是要和文档的元数据设计一起考虑的？
speaker 2: 完全正确，必须在文档入库的时候就打上正确的部门、角色等元数据标签，检索的时候才能据此做出正确的过滤，这也是权限感知 RAG 能够落地的基础。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
