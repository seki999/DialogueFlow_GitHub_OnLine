speaker 1: 之前提到 LangGraph 里有个很重要的概念叫 State，这个具体是指什么？
speaker 2: State 就是整个 Agent 运行过程中的状态数据，比如可以包含 messages 对话记录、user 用户信息、documents 检索到的文档、tool_results 工具执行结果，还有 retry_count 重试次数这些字段。
speaker 1: 那这个状态是怎么在流程中传递和变化的？
speaker 2: 每个 Node 节点的基本工作方式是：先读取当前的 State，进行相应的处理，然后修改 State，再把更新后的 State 传给下一个 Node，就像接力棒一样一步步传递下去。
speaker 1: 为什么要用这种基于状态的方式，而不是直接让 LLM 自己决定接下来怎么做？
speaker 2: 因为完全让 LLM 自主决定下一步，虽然听起来更“智能”，但实际上很难预测和控制，一旦出错很难排查原因；而基于明确 State 的流程设计，每一步的输入输出都清晰可见，可靠性要高得多。
speaker 1: 所以企业更看重的其实不是“越自主越好”？
speaker 2: 对，企业普遍更看重可控、可解释、可审计、可重现这几个特性，而不是单纯追求 Agent 有多“自主”。
speaker 1: 那实际项目里，通常是怎么平衡自主性和可控性的？
speaker 2: 一个常见的经验法则是，让大约 80% 的流程走确定性的工作流，只在剩下 20% 真正需要判断和决策的地方，才交给 LLM 自主决策，而不是让整个系统 100% 完全自主运行。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
