speaker 1: 向量数据库这么多选择，FAISS、Chroma、Qdrant、Milvus、Pinecone、pgvector，第一次接触的话该怎么选？
speaker 2: 先明确使用场景。FAISS 适合本地快速实验，Chroma 开发起来非常方便，适合快速搭建原型；如果要面向生产环境，Qdrant 是很受欢迎的选择；如果数据规模特别大，Milvus 会更合适；Pinecone 是完全托管的 SaaS 服务，不用自己运维。
speaker 1: 那 pgvector 呢？听起来好像和其他几个不太一样。
speaker 2: pgvector 其实是 PostgreSQL 的一个扩展，让你可以直接在 PostgreSQL 里存储和检索向量数据，这一点对企业特别实用。
speaker 1: 为什么对企业特别实用？
speaker 2: 因为企业本来就大量使用 PostgreSQL 作为业务数据库，如果向量数据也能存在同一个数据库系统里，运维、备份、权限管理都可以复用已有的体系，不需要再额外维护一套独立的向量数据库。
speaker 1: 所以选型的关键，其实不是看哪个技术最新，而是看企业现有的基础设施？
speaker 2: 完全正确。很多团队会盲目追求最新最炫的向量数据库，但如果企业已经有成熟的 PostgreSQL 运维团队，选择 pgvector 往往比引入一个全新系统更稳妥、更容易长期维护。
speaker 1: 那向量数据库选好了，是不是检索效果就有保障了？
speaker 2: 还不够，向量数据库只是整个链条中的一环，它的效果高度依赖前面 Chunking 切分得好不好、Embedding 模型选得对不对，这些前置环节没做好，再好的向量数据库也检索不出高质量的结果。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
