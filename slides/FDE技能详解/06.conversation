speaker 1: 如果客户说数据完全不能离开公司内部，那企业 Agent 是不是就没法用了？
speaker 2: 不是没法用，而是要换一种部署方式，也就是本地大模型部署，也叫 On-Premise 部署。
speaker 1: 常见的本地部署工具有哪些？
speaker 2: 比如 Ollama、vLLM、LM Studio、llama.cpp，还会用到 Hugging Face 上的开源模型，以及 GGUF、AWQ、GPTQ 这些量化格式。
speaker 1: 量化格式是做什么用的？
speaker 2: 简单说，量化是为了把模型压缩得更小、跑得更快，比如从 FP16 压缩到 INT8 甚至 INT4，这样在有限的 GPU 显存下也能跑得动大模型。
speaker 1: 那部署的时候还需要关注哪些技术指标？
speaker 2: 需要关注 GPU VRAM 显存大小、量化精度、KV Cache 的管理、Batch 批处理大小，以及最终的吞吐量和延迟表现，这些直接决定了模型能不能满足企业的响应速度要求。
speaker 1: 如果要落地一个实际的部署路径，大概是什么样子？
speaker 2: 典型路径是选一个开源模型，比如 Qwen、Llama 或 Mistral，部署到 GPU 服务器上，用 vLLM 这类推理框架对外提供一个 OpenAI 兼容的 API 接口。
speaker 1: 这样一来，企业内部的 Agent 就可以像调用 OpenAI 一样调用这个本地服务？
speaker 2: 对，Agent 完全不需要访问外部模型 API，所有请求都在企业内部完成，这也是很多对数据安全高度敏感的客户最终选择的方案。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
