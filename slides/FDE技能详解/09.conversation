speaker 1: 我看很多代码示例里，Chunking 就是设置一下 chunk_size=1000，chunk_overlap=200，这样是不是就可以了？
speaker 2: 这种做法对入门 Demo 来说可以跑通，但在真正的企业项目里往往远远不够，因为它完全忽略了文档本身的结构和语义。
speaker 1: 那有哪些更合适的切分策略？
speaker 2: 常见的有固定切分、递归切分、语义切分、文档结构切分，还有父子切分这几种，各自适合不同类型的文档。
speaker 1: 能不能举个例子说明为什么固定切分不够好？
speaker 2: 比如一份技术文档，结构是产品下面分型号，型号下面分模块，模块下面分故障，故障下面又对应解决方案。如果按照固定字符数切分，很可能把“故障”和它对应的“解决方案”硬生生切到两个不同的 chunk 里。
speaker 1: 那样的话，检索的时候会出现什么问题？
speaker 2: 检索出来的内容可能只包含故障描述，却丢失了对应的解决方案，导致模型没有完整信息可以参考，最终答案质量就会下降。
speaker 1: 所以更好的做法是尽量保留文档原本的层级结构？
speaker 2: 对，比如可以按照文档结构切分，或者用父子切分的方式，让每个小片段都能关联到它所在的更大上下文，这样检索出来的内容才更完整、更有意义。
speaker 1: 看来 Chunking 虽然听起来是个很基础的步骤，但其实很考验对文档结构的理解。
speaker 2: 确实如此，这也是为什么很多团队即使用了很好的 Embedding 模型和向量数据库，效果依然不理想，往往问题就出在 Chunking 这一步没有做好。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
