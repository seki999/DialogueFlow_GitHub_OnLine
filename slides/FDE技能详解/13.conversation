speaker 1: 如果检索先召回了 50 条结果，是不是可以直接把这 50 条一起丢给大模型，让它自己判断哪些有用？
speaker 2: 理论上可以，但实际效果通常不好，一方面上下文会变得很长、成本升高，另一方面很多不太相关的内容会干扰模型的判断。
speaker 1: 那更好的做法是什么？
speaker 2: 生产环境里更常见的做法是，先用 Retriever 召回比如 Top 50 条结果，然后用一个专门的 Reranker 模型对这 50 条重新打分排序，挑出最相关的 Top 5，再交给 LLM 生成答案。
speaker 1: 为什么不直接让第一步的检索就召回最精准的 5 条？
speaker 2: 因为初步检索通常追求的是“召回率”，也就是尽量不漏掉相关内容，所以会先召回一个相对宽泛的候选集合；而 Reranker 更擅长精细地判断“这条内容和问题到底有多相关”，两者分工不同。
speaker 1: 常见的 Reranker 有哪些？
speaker 2: 比如 BGE Reranker、Cohere Rerank，还有一类叫 Cross Encoder 的模型架构，它们都可以对候选结果重新精确打分。
speaker 1: 所以 Embedding 和 Reranking 其实是两个不同阶段的问题？
speaker 2: 对，很多人容易把两者混为一谈，但其实 Embedding 解决的是“怎么快速从海量文档里找出候选集合”，而 Reranking 解决的是“在候选集合里，谁才是真正最相关的”，两者结合起来才能既快又准。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
