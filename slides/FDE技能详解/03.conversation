speaker 1: 看到这张十层的技术栈图，说实话有点压力，是不是每一层都要精通才能算合格的 FDE？
speaker 2: 不需要每一层都精通，但至少要理解其中八层左右，并且最关键的是要能把它们组合起来，变成一个能跑的生产系统。
speaker 1: 那这十层具体是哪些？能不能按顺序说一下？
speaker 2: 从底层往上，依次是软件工程、LLM、Prompt 和 Context Engineering、RAG 也就是知识检索、Agent、工具与 MCP、Workflow 也就是 LangGraph 这一类编排框架、基础设施和云、安全与治理，最后是评估与可观测性。
speaker 1: 感觉最底下的软件工程和最上面的评估，看起来关联不大，为什么会放在同一张图里？
speaker 2: 因为它们其实是一条链条。软件工程是地基，没有扎实的工程能力，上面所有的 AI 能力都搭不稳；而评估和可观测性则是保证这个系统在生产环境里持续可靠运行的最后一道防线。
speaker 1: 所以这十层更像是一个从底层到顶层，逐渐从技术走向可信度的过程？
speaker 2: 可以这么理解。中间的 LLM、RAG、Agent、工具、Workflow 是核心能力层，负责把智能变成行动；而基础设施、安全和评估则是让这套系统能在真实企业环境里长期稳定运行的支撑层。
speaker 1: 那我们接下来会按照这个顺序，一层一层地深入讲解吗？
speaker 2: 对，从下一章开始，我们会先从最基础的软件工程讲起，然后依次进入 LLM、RAG、Agent，一路讲到安全和评估，把这十层完整地串联起来。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
