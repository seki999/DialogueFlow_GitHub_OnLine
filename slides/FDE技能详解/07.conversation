speaker 1: 网上很多教程讲 RAG，好像就是把 PDF 转成向量存进数据库，然后丢给 LLM，这样就够了吗？
speaker 2: 在企业场景里，这个简化版流程远远不够。真正生产级的 RAG 要经过解析、清洗、切分、加元数据、向量化、存入向量数据库，然后是混合检索、重排序、检索结果整理、构建上下文，最后才是交给 LLM 生成答案，还要附带引用来源。
speaker 1: 步骤一下子多了这么多，是不是每一步都很重要？
speaker 2: 对，而且任何一步做得不好，都会拖累最终效果。比如解析没做好，表格数据可能全部乱掉；切分没做好，语义会被打断；没有重排序，检索回来的内容可能相关性很差。
speaker 1: 这么看的话，RAG 的核心难点其实不在最后调用 LLM 那一步？
speaker 2: 完全正确，最后调用 LLM 生成答案反而是相对简单的一步，真正决定效果好坏的是前面这一长串数据处理和检索优化流程。
speaker 1: 那我们接下来会一步步拆解这些环节吗？
speaker 2: 会的，接下来几章会依次讲解企业数据处理的难点、Chunking 切分策略、Embedding 向量化、向量数据库的选型、Hybrid Search 混合检索、Reranker 重排序，以及权限感知的检索设计。
speaker 1: 感觉这一整条流程更像是一个数据工程流水线，而不是单纯的 AI 问题。
speaker 2: 非常准确的总结，企业 RAG 本质上是数据工程加检索工程加生成能力的综合体，这也是为什么它值得单独用好几章来讲清楚。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
