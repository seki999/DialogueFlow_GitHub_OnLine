speaker 1: 既然 Embedding 向量检索这么强大，能理解语义，是不是就不需要传统的关键词搜索了？
speaker 2: 不完全是。举个例子，如果用户搜索的是一个具体的错误代码，比如 ORA-12514，这种场景下纯语义检索不一定是最优选择。
speaker 1: 为什么？错误代码不也是文字吗，应该也能转成向量吧。
speaker 2: 可以转成向量，但错误代码这种由字母数字组成的精确标识符，语义信息很有限，向量检索更擅长捕捉“意思相近”，而不是“字符完全匹配”，这时候传统的关键词检索，比如 BM25，反而可能更准确地命中相关文档。
speaker 1: 所以两种检索方式各有各的擅长领域？
speaker 2: 对，语义检索擅长理解“这句话大概在说什么”，关键词检索擅长精确匹配“这个词具体出现在哪里”，企业级 RAG 通常会把两者结合起来，也就是 Hybrid Search 混合检索。
speaker 1: 具体是怎么结合的？
speaker 2: 通常是同时用 Vector Search 和 BM25 各自检索一遍，然后把两边的结果融合起来，这样既能覆盖语义相近的内容，也不会漏掉精确匹配的关键信息。
speaker 1: 如果只用其中一种方式，会有什么风险？
speaker 2: 如果只用向量检索，可能会漏掉像错误代码这样需要精确匹配的内容；如果只用关键词检索，又可能漏掉那些用不同措辞表达同一个意思的相关文档，所以 Hybrid Search 的组合方式在企业场景里几乎是标配。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
