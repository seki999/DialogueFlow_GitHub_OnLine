speaker 1: 上一章说 Agent 最重要的能力是行动，那这个“行动”具体是靠什么机制实现的？
speaker 2: 核心机制就是 Tool Calling，也就是工具调用。LLM 本身只能生成文字，它没办法直接去查数据库或者发消息，必须通过调用外部工具才能真正“动手”做事。
speaker 1: 常见的工具类型都有哪些？
speaker 2: 范围其实很广，包括 SQL、REST API、浏览器操作、Python 代码执行、文件系统操作、发邮件，还有 Slack、Teams、Jira、GitHub 这些协作工具，甚至 Salesforce、ServiceNow、SAP 这些企业系统，以及 AWS、Kubernetes 这类基础设施。
speaker 1: 这么多种类的工具，LLM 是怎么知道该在什么时候调用哪一个的？
speaker 2: 这就要靠模型的工具调用能力，也就是让 LLM 根据当前任务的描述，判断应该使用哪个工具、传入什么参数，然后系统实际执行这个工具调用，再把结果返回给 LLM 继续处理。
speaker 1: 如果 LLM 判断错了，选错了工具或者传错了参数，会怎么样？
speaker 2: 那就可能导致任务执行失败，甚至产生错误的副作用，比如误删数据或者发错消息，所以工具的设计、参数的校验、以及执行结果的观察反馈，都需要非常谨慎地处理。
speaker 1: 所以 Tool Calling 不只是“调用一个函数”这么简单？
speaker 2: 对，它其实串联起了整个 Agent 的能力，也可以说，Agent 的能力本质上就是 LLM 加上这些工具，再加上记忆和工作流的组合，Tool Calling 是让这个组合真正运转起来的关键环节。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
