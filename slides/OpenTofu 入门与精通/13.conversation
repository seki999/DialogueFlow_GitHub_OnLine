speaker 1: 如果我想一次创建三台一模一样的实例，应该怎么写？
speaker 2: 可以用 count，比如 count = 3，然后用 count.index 拼接名字，像 "app-${count.index + 1}"，生成的资源地址是 oci_core_instance.app[0]、[1]、[2]。
speaker 1: 这种方式有什么问题吗？
speaker 2: 问题在于，如果你删除列表中间的一个元素，后面的索引可能会跟着变化，导致 OpenTofu 认为其他没有变化的资源也需要重建，这在生产环境是比较危险的。
speaker 1: 那有没有更稳妥的方式？
speaker 2: 有，就是 for_each。比如定义一个 subnets 变量，类型是 map(object({cidr = string, public = bool}))，里面有 web、app、db 三个 key，各自带着不同的 CIDR 和是否公网的配置。
speaker 1: 创建出来的资源地址长什么样？
speaker 2: 地址是 oci_core_subnet.this["web"]、["app"]、["db"]，用 key 而不是数字索引，就算你删掉中间的 app，web 和 db 的地址也不会受影响。
speaker 1: 所以生产环境更推荐用 for_each？
speaker 2: 对，生产环境通常更推荐 for_each，因为资源地址稳定；count 更适合那种同质、没有业务含义名字的简单场景。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
