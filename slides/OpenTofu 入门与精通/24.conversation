speaker 1: 代码写完了，提交之前有没有什么建议的检查步骤？
speaker 2: 有，建议每次提交前运行三步：tofu fmt -check -recursive、tofu init -backend=false，还有 tofu validate。
speaker 1: 为什么 init 要加 -backend=false？
speaker 2: 因为在只做语法和格式检查的阶段，不需要真正连接 Backend，加上这个参数可以跳过需要真实凭证的初始化步骤，检查更轻量。
speaker 1: 如果要做更完整的 CI 检查呢？
speaker 2: 可以加入 TFLint、Checkov、Trivy config、OPA/Conftest，还有自定义的命名和标签检查，这些工具能发现更深层的问题，比如安全配置、命名规范等。
speaker 1: 文中给的 GitHub Actions 示例大概是什么流程？
speaker 2: 先 checkout 代码，再用 opentofu/setup-opentofu 安装指定版本的 OpenTofu，然后依次跑 Format check、Initialize、Validate 三个步骤。
speaker 1: 那实际的 plan 是不是也能直接放进 CI？
speaker 2: 可以，但需要 OCI 凭证，所以要配置成受控环境，并且要避免来自不可信 Fork 的代码直接读取生产环境的 Secrets，这是安全上必须注意的地方。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
