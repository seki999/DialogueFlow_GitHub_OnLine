speaker 1: 网络建好了，现在要在里面创建虚拟机了？
speaker 2: 对，创建 Compute 实例需要五样东西：Availability Domain、Subnet OCID、Image OCID、Shape，还有 SSH Public Key。
speaker 1: Availability Domain 怎么拿到？
speaker 2: 用 data.oci_identity_availability_domains 查询，跟之前查 Region 列表的思路一样。
speaker 1: Image OCID 是不是可以直接抄网上找到的一个固定值？
speaker 2: 最好不要，因为镜像 OCID 通常具有区域性，换一个 Region 很可能就找不到了。更稳妥的做法是用 data.oci_core_images 动态查询，按 sort_by TIMECREATED、sort_order DESC 排序，取 images[0] 拿到最新的镜像。
speaker 1: Shape 这块有什么需要注意的？
speaker 2: 如果用 Flexible Shape，比如 VM.Standard.E5.Flex，还要额外写一个 shape_config 块，指定 ocpus 和 memory_in_gbs，否则光写 Shape 名称是不够的。
speaker 1: Compute 起来之后是不是就是一台空的虚拟机？
speaker 2: 不完全是，示例里的 metadata 中有一段 Cloud-Init 脚本，会自动安装 nginx，启用并启动服务，还写入一行简单的 HTML 首页，虚拟机一启动就能直接访问。
speaker 1: 怎么拿到这台虚拟机的公网 IP？
speaker 2: 通过 data.oci_core_vnic_attachments 和 data.oci_core_vnic 两个 Data Source 查询，再用 output 把 public_ip_address 暴露出来。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
