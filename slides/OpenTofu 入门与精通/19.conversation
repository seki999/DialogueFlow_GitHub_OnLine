speaker 1: 如果不小心执行了 tofu destroy，把生产数据库删了怎么办？
speaker 2: 这正是 lifecycle 块里 prevent_destroy 要解决的问题。给资源加上 lifecycle { prevent_destroy = true }，之后即使运行 tofu destroy，也会因为这个资源而失败。
speaker 1: 那是不是加了这个就绝对安全了？
speaker 2: 不是绝对安全，它只是 OpenTofu 侧的保护，不是 OCI Console 或 API 层面的绝对保护，如果有人直接在 Console 里删除，这个保护是挡不住的。
speaker 1: lifecycle 块里还有别的选项吗？
speaker 2: 有 ignore_changes，用来忽略外部系统对某些属性的修改，比如 freeform_tags 里某个由外部系统管理的 key。
speaker 1: 是不是干脆写个 ignore_changes = all 更省事？
speaker 2: 千万别这么用，这样会让 OpenTofu 几乎失去检测漂移的意义，任何变化都不会被发现了。
speaker 1: 还有 create_before_destroy 是什么意思？
speaker 2: 适合需要新旧资源并行存在的场景，先创建新资源再删除旧资源。但用之前要确认资源名称是否允许重复、配额是否够、IP 或 DNS 会不会冲突。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
