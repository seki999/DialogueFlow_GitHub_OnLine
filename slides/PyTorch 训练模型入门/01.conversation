speaker 1: 我一直分不清训练模型到底在做什么，能先讲讲这个核心概念吗？
speaker 2: 可以。训练模型说到底就是一句话：找到一组参数，让模型的预测结果尽可能接近真实答案。
speaker 1: 那具体是怎么衡量“接近”的？
speaker 2: 用损失函数。比如用均方误差，把预测值和真实值的差平方后取平均，差越大损失就越大。
speaker 1: 图里画的前向传播、计算损失、反向传播、更新参数，是不是就是训练的完整循环？
speaker 2: 对，就是这四步不断循环。前向传播是用当前参数算出预测值。
speaker 1: 然后计算损失是拿预测值和真实值比较？
speaker 2: 没错。接下来反向传播会用链式法则，算出损失对每个参数的偏导数，也就是梯度。
speaker 1: 梯度是不是就是告诉我们往哪个方向调参数？
speaker 2: 对，梯度告诉我们损失增长最快的方向，我们要往相反方向走，所以更新公式里有个负号。
speaker 1: 公式里的学习率 lr 是干什么的？
speaker 2: lr 决定每次调整参数的步子大小。步子太大容易跳过最优点，太小又收敛得很慢。
speaker 1: 图里那个房价预测的例子挺直观的，一开始 w 和 b 是随机的吧？
speaker 2: 对，一开始随便设的，比如 w=0.3，b=10，这时候预测的房价肯定很不准。
speaker 1: 那是不是就靠不断重复这四步，参数才会慢慢变准？
speaker 2: 是的，每重复一轮，w 和 b 都会往损失变小的方向挪一点，重复足够多次后就会接近真实的规律，比如 w 接近 3、b 接近 2 这种效果。
speaker 1: 明白了，所以后面复杂的神经网络，本质上也是这四步在反复执行？
speaker 2: 完全正确，不管模型多复杂，训练的核心永远是这四步：前向传播、计算损失、反向传播、更新参数。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
