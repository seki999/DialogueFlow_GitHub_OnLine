speaker 1: 之前分类模型用的都是全连接层，处理图像的话是不是不太合适？
speaker 2: 对，全连接层会把图像展平成一维向量，丢失了像素之间的空间位置关系，图像任务更常用 CNN。
speaker 1: CNN 具体是怎么提取图像特征的？
speaker 2: 通过卷积核在图像上滑动，提取边缘、纹理、形状这些局部结构，层数越深，提取的语义信息也越高级。
speaker 1: 图像输入的形状是不是也有个固定约定？
speaker 2: 对，常见约定是 [N, C, H, W]，分别是批次大小、通道数、高度、宽度，比如 MNIST 就是 [128, 1, 28, 28]。
speaker 1: 图里网络结构是 Conv2d、BatchNorm2d、ReLU、MaxPool2d 重复了两次，这个组合是标准搭配吗？
speaker 2: 是的，Conv2d 负责提取特征，BatchNorm2d 让训练更稳定，ReLU 引入非线性，MaxPool2d 负责下采样、减小特征图尺寸。
speaker 1: MaxPool2d(2) 具体做了什么？
speaker 2: 它在局部窗口内取最大值，把特征图的高和宽都缩小一半，既降低了计算量，也让模型对图像的小幅平移更不敏感。
speaker 1: 特征提取完之后接的 Flatten 是做什么用的？
speaker 2: 因为卷积部分输出的是多维的特征图，而全连接层要求输入是二维的，所以要用 Flatten 把它展平成一维向量。
speaker 1: 展平之后就可以直接接全连接层做分类了？
speaker 2: 对，图里最后用了 Linear、ReLU、Dropout、Linear 这样一个小的分类头，输出对应类别数量的 logits。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
