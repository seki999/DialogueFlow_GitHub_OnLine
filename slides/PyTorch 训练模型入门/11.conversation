speaker 1: 训练日志里经常看到 epoch 和 iteration 这两个词，它们是一回事吗？
speaker 2: 不是一回事，还要加上 batch，这三个概念放在一起理解会更清楚。
speaker 1: 先说 batch 吧，它是什么？
speaker 2: batch 就是一次送进模型的数据量，大小由 batch_size 决定，比如设置 100，就是一次处理 100 条数据。
speaker 1: 那 iteration 呢？
speaker 2: iteration 就是完成一次“前向传播、反向传播、参数更新”，处理完一个 batch 就是一次 iteration。
speaker 1: epoch 又是什么？
speaker 2: epoch 是指整个训练集被完整用了一遍，通常要经过很多个 epoch 模型才会收敛。
speaker 1: 图里举的例子是 1000 条数据，batch_size 是 100，一个 epoch 有多少个 iteration？
speaker 2: 用 1000 除以 100，正好是 10，所以一个 epoch 里会执行 10 次 iteration。
speaker 1: 如果数据条数不能被 batch_size 整除呢，比如 1030 条？
speaker 2: 那前 10 个 batch 各 100 条，剩下 30 条组成最后一个不满的 batch，所以这时候是 11 个 iteration。
speaker 1: 所以如果我把 batch_size 调大，iteration 的数量是不是会变少？
speaker 2: 对，数据总量不变的情况下，batch_size 越大，需要的 iteration 次数就越少，但每次计算量也会更大。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
