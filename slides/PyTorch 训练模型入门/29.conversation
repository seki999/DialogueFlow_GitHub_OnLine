speaker 1: 前面把训练流程的各个环节都讲完了，实际训练中出问题应该怎么排查？
speaker 2: 这一章就专门整理了四类最常见的问题：Loss 不下降、Loss 变 NaN、准确率不变、GPU 利用率低。
speaker 1: 先说 Loss 完全不下降，应该按什么顺序排查？
speaker 2: 先看数据和标签是不是对应的，再检查参数 requires_grad 是不是 True，然后看有没有漏写 loss.backward() 或者 optimizer.step()，最后再看学习率和 shape 是否正确。
speaker 1: 这些前面章节其实都提到过？
speaker 2: 对，这一章相当于把之前分散讲过的注意事项串成一个排查清单。
speaker 1: 那 Loss 变成 NaN 呢，一般是什么原因？
speaker 2: 常见原因有学习率太大、除以零、log(0)、指数溢出、梯度爆炸，或者输入数据本身就已经包含 NaN。
speaker 1: 有什么办法快速定位是哪一步出的问题？
speaker 2: 可以加断言，比如 assert torch.isfinite(inputs).all()，先确认输入本身没问题，再逐个参数检查梯度是否还是有限值。
speaker 1: 训练准确率一直不变又是什么情况？
speaker 2: 要检查标签是不是从 0 开始编码、argmax 用的维度对不对、输出层类别数设置得对不对，还有参数是不是被意外冻结了。
speaker 1: GPU 利用率低是不是也有类似的排查思路？
speaker 2: 有，常见原因是数据读取慢、num_workers 太少、batch_size 太小，或者频繁调用 .item() 和打印日志拖慢速度，可以试试增大 num_workers、开启 pin_memory。
speaker 1: 所以这一章更像是把之前学到的知识用在实战排查上？
speaker 2: 对，理解每个环节的原理是基础，能快速定位问题才是真正把知识用起来了。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
