speaker 1: 深层网络训练中经常听说梯度消失和梯度爆炸，它们是同一个问题吗？
speaker 2: 不是，方向正好相反，但都和反向传播的链式法则有关。
speaker 1: 先说梯度消失，具体是什么现象？
speaker 2: 就是越靠近输入层，梯度越小，几乎接近 0，导致这些层的参数几乎学不到东西。
speaker 1: 为什么会出现这种情况？
speaker 2: 常见原因是网络太深、用了 Sigmoid 或 Tanh 这类容易饱和的激活函数，或者初始化不合理。
speaker 1: 有什么办法解决吗？
speaker 2: 可以换成 ReLU 或 GELU，加入残差连接，用 BatchNorm 或 LayerNorm，或者用更合理的初始化方式。
speaker 1: 那梯度爆炸又是什么样子？
speaker 2: 正好相反，梯度值变得极大，训练会很不稳定，严重时直接出现 NaN，Transformer 和 RNN 里比较常见。
speaker 1: 应对梯度爆炸最常用的方法是什么？
speaker 2: 梯度裁剪，用 torch.nn.utils.clip_grad_norm_，把梯度的范数限制在一个上限以内，比如 max_norm=1.0。
speaker 1: 这行代码应该写在训练循环的哪个位置？
speaker 2: 要写在 loss.backward() 之后、optimizer.step() 之前，因为要先算出梯度，再对它做裁剪，最后才用裁剪后的梯度去更新参数。
speaker 1: 所以这两个问题一个是梯度太小，一个是梯度太大，解决思路也完全不同？
speaker 2: 对，梯度消失更多靠结构上的改进，比如激活函数和残差连接；梯度爆炸更多靠训练时的直接限制，比如梯度裁剪。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
