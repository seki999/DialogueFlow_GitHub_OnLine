speaker 1: 前面训练循环里一直有 device 这个变量，具体应该怎么选择设备？
speaker 2: 标准写法是先判断 GPU 是否可用，可用就用 GPU，不可用再退回 CPU，图里画的就是这个优先级。
speaker 1: 如果是 Mac 的 M 系列芯片呢，也能用上 GPU 加速吗？
speaker 2: 可以，PyTorch 支持 MPS 后端，可以用 torch.backends.mps.is_available() 检测是否可用，作为 CUDA 和 CPU 之间的一个选项。
speaker 1: 选好设备之后，具体要怎么用？
speaker 2: 模型和输入数据都要调用 .to(device)，确保它们在同一个设备上，这也是之前反复强调的重点。
speaker 1: 如果没做好会怎么样？
speaker 2: 就会报 “Expected all tensors to be on the same device”，比如模型在 GPU 上，但输入数据还留在 CPU，就会触发这个错误。
speaker 1: 图里还提到了 pin_memory，这是什么？
speaker 2: pin_memory=True 会把 DataLoader 加载的数据放进固定内存，这样从 CPU 传到 CUDA GPU 的速度会更快。
speaker 1: 那 non_blocking=True 又是干什么的？
speaker 2: 它可以让数据拷贝异步进行，不阻塞后续计算，但要真正发挥效果，通常需要配合 pin_memory=True 一起使用。
speaker 1: 所以这几个设置其实是配合在一起，共同提升数据传输效率的？
speaker 2: 对，尤其是在 GPU 利用率不高、数据传输成为瓶颈的场景下，这些设置能带来明显的速度提升。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
