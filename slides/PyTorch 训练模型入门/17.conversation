speaker 1: 上一章讲了几种优化器，学习率本身是不是也可以在训练过程中变化？
speaker 2: 对，学习率通常是最重要的超参数，而且很多项目会用调度器让它随训练过程动态调整。
speaker 1: 先说说学习率太大或太小会怎样？
speaker 2: 太大的话 Loss 会剧烈震荡、不下降，严重时甚至变成 NaN；太小的话训练会很慢，容易长时间停留在较差区域。
speaker 1: 图里第一个环节是 Warmup，这是做什么的？
speaker 2: Warmup 是训练刚开始时，让学习率从一个较小值逐渐增加到峰值，因为参数一开始是随机的，直接用大学习率容易不稳定。
speaker 1: 达到峰值之后呢？
speaker 2: 之后通常会配合某种衰减策略，比如 StepLR，每隔固定的 Epoch 数，把学习率乘以一个系数往下降。
speaker 1: CosineAnnealing 又是什么样的下降方式？
speaker 2: 它让学习率按照余弦曲线的形状平滑地往下降，是目前比较常见的一种平滑衰减策略。
speaker 1: 那 ReduceLROnPlateau 有什么不同？
speaker 2: 它不是按固定轮数下降，而是持续监控验证指标，比如验证损失，如果连续几轮都没有改善，就自动把学习率调低。
speaker 1: 使用这个调度器的时候，代码上要注意什么？
speaker 2: 要记得调用 scheduler.step(validation_loss)，把验证损失传进去，它才知道该不该降低学习率。
speaker 1: 微调预训练模型的时候，学习率一般设多大？
speaker 2: 通常会比从零训练小很多，大概在 1e-5 到 5e-5 之间，因为预训练参数已经比较接近合理值了，不需要大幅调整。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
