speaker 1: 前面训练循环都是写在一个大的 for 循环里，正式项目会不会拆得更清晰一些？
speaker 2: 会的，通常会拆成 train_one_epoch 和 evaluate 两个独立函数，职责更清楚，也方便复用。
speaker 1: train_one_epoch 里第一行是 model.train()，这是必须的吗？
speaker 2: 是的，这一步会让 Dropout、BatchNorm 这些训练和推理行为不同的模块切换成训练模式，比如 Dropout 才会真正随机丢弃部分神经元。
speaker 1: evaluate 函数上面加了个 @torch.inference_mode() 装饰器，这是干什么的？
speaker 2: 它会告诉 PyTorch 这段代码不需要构建反向传播用的计算图，能节省内存和计算，配合里面的 model.eval() 一起用。
speaker 1: 我注意到累加损失用的是 loss.item() * inputs.size(0)，为什么不直接把 loss 加起来？
speaker 2: 因为 loss_function 返回的是当前 batch 的平均损失，如果各个 batch 大小不完全一样，直接加会不准确，所以要先乘以样本数还原成总损失，最后再除以总样本数。
speaker 1: 用 loss.item() 而不是直接用 loss 变量，又是为什么？
speaker 2: loss.item() 会取出一个普通的 Python 数值，安全累加不会有问题；直接用 loss 累加，可能会把整个带梯度历史的计算图保留下来，导致内存不断增长。
speaker 1: 主循环里还有一步是比较当前验证损失和历史最优，这是做什么用的？
speaker 2: 是为了保存在验证集上表现最好的那一版模型，因为训练到后期模型可能已经开始过拟合，最后一轮不一定是最好的一轮。
speaker 1: 所以整个规范流程就是每轮训练完立刻验证，再根据验证结果决定要不要保存模型？
speaker 2: 完全正确，这也是绝大多数专业训练脚本的标准结构。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
