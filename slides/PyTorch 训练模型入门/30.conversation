speaker 1: 我们把整个 PyTorch 训练的知识都过了一遍，能不能帮我串一下整体脉络？
speaker 2: 可以。最核心的是那四步循环：前向传播、计算损失、反向传播、更新参数，后面所有内容都是围绕这四步展开的。
speaker 1: 六大组件是不是就是支撑这四步运转的基础设施？
speaker 2: 对，Tensor 保存数据和梯度，Dataset 和 DataLoader 负责把数据喂给模型，nn.Module 定义模型结构，Loss 衡量误差，Optimizer 负责更新参数。
speaker 1: 后面讲的分类、CNN、迁移学习，是不是就是在这个基础上扩展模型能力？
speaker 2: 没错，从最简单的线性回归，到多分类、二分类，再到用 CNN 处理图像，用迁移学习复用已经训练好的知识，模型能力在一步步提升。
speaker 1: 那正则化、归一化、梯度问题这些内容呢？
speaker 2: 这些都是为了让训练更稳定：正则化对付过拟合，归一化让训练更平滑，梯度消失和爆炸的应对方法保证深层网络能正常训练。
speaker 1: 我印象比较深的一个常见误区是什么？
speaker 2: 一个很典型的误区是使用 CrossEntropyLoss 或 BCEWithLogitsLoss 时，手动再做一次 Softmax 或 Sigmoid，这其实是重复计算，会让训练出错。
speaker 1: 还有一个是不是关于梯度清零的？
speaker 2: 对，很多人容易忘记每轮训练前调用 zero_grad，梯度默认是累加的，不清空会导致更新方向完全错误。
speaker 1: 如果我想继续往下学，比如学 Transformer，之前学的这些知识还有用吗？
speaker 2: 非常有用，Transformer 里的训练循环、损失函数、优化器选择、归一化方式，几乎全部建立在我们学过的这些基础之上。
speaker 1: 那如果以后要训练特别大的模型，一张 GPU 放不下怎么办？
speaker 2: 可以了解一下 DDP 和 FSDP 这类分布式训练方案，DDP 适合模型能放进单卡的情况，FSDP 则是把参数、梯度、优化器状态都分片到多张卡上。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。这也是整个课程的最后一章，接下来可以完成本章末尾的五道综合测验题，检验一下自己对整个课程的掌握程度。
