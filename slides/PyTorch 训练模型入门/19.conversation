speaker 1: 上一章讲到要根据验证损失保存最优模型，具体怎么判断模型状态好不好？
speaker 2: 这就要看训练损失和验证损失的走势了，主要分两种情况：欠拟合和过拟合。
speaker 1: 先说欠拟合吧，它长什么样？
speaker 2: 欠拟合的表现是训练集效果差，验证集效果也差，说明模型连训练数据本身都没学好。
speaker 1: 那过拟合呢？
speaker 2: 过拟合正好相反，训练损失一直在下降，看起来学得很好，但验证损失却先降后升，说明模型开始死记硬背训练数据了。
speaker 1: 遇到欠拟合应该怎么处理？
speaker 2: 可以增加模型容量、延长训练时间、改善输入特征，或者调整学习率，总之是让模型有更强的能力去拟合数据。
speaker 1: 那过拟合又该怎么应对？
speaker 2: 思路正好相反，可以增加训练数据、做数据增强、加 Dropout、加 Weight Decay，或者用 Early Stopping 及时停下来。
speaker 1: 图里那条典型曲线，训练损失一直降、验证损失先降后升，最佳保存点应该选在哪？
speaker 2: 应该选在验证损失最低的那个点，之后继续训练，模型只是在训练集上表现更好，但泛化能力其实在变差。
speaker 1: 所以判断思路其实很简单，看训练集和验证集各自的表现就够了？
speaker 2: 对，训练集差验证集也差是欠拟合，训练集好验证集差是过拟合，两者都好才是理想状态。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
