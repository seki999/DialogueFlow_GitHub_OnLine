speaker 1: 前面几章的模型都用了 ReLU，如果没有它，直接堆叠好几个 Linear 层可以吗？
speaker 2: 数学上可以运行，但意义不大，因为多个线性层叠加，仍然等价于一层线性变换，写成 Wx+b 的形式。
speaker 1: 所以激活函数就是为了打破这种线性叠加？
speaker 2: 对，激活函数给模型引入了非线性，让深层网络真正具备拟合复杂规律的能力。
speaker 1: 图里的网络结构是 Linear、ReLU、Dropout 重复了两次，最后再接一层 Linear，这是常见写法吗？
speaker 2: 是的，这是很典型的多层感知机结构，ReLU 负责非线性，Dropout 负责正则化，防止过拟合。
speaker 1: ReLU 具体是怎么计算的？
speaker 2: 公式很简单，max(0, x)，输入是负数就变成 0，是正数就保持不变。
speaker 1: 我听说过“死亡 ReLU”这个说法，是什么意思？
speaker 2: 如果某个神经元的输入长期是负数，ReLU 输出就一直是 0，梯度也是 0，这个神经元实际上就不再更新了，好像“死掉”了一样。
speaker 1: 那有什么办法缓解这个问题？
speaker 2: 可以用 LeakyReLU，它在负数区域保留一个很小的斜率，而不是完全截断为 0，这样神经元还有机会恢复。
speaker 1: 那 Sigmoid 和 Tanh 呢，什么时候会用到？
speaker 2: Sigmoid 输出在 0 到 1 之间，常用于二分类的概率输出；Tanh 输出在负1到1之间，在一些循环网络里还会用到。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
