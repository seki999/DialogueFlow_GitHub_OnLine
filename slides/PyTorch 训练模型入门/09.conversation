speaker 1: 前面训练循环用的数据都是笼统的 x、y，实际项目是不是要把数据分成几部分？
speaker 2: 对，通常要分成训练集、验证集和测试集三部分，各自的职责不一样。
speaker 1: 训练集的作用应该是用来更新参数吧？
speaker 2: 没错，训练集是唯一参与梯度计算和参数更新的数据。
speaker 1: 那验证集是做什么用的？
speaker 2: 验证集不参与参数更新，主要用来在训练过程中评估当前模型效果，比如选模型、调超参数、判断有没有过拟合。
speaker 1: 图里验证集还连了 Early Stopping，这是什么意思？
speaker 2: 就是当验证集上的表现不再改善时，提前停止训练，避免继续训练导致过拟合。
speaker 1: 那测试集又是什么时候用？
speaker 2: 测试集只在模型开发基本完成之后用一次，用来评估模型真正的泛化能力。
speaker 1: 为什么强调只用一次，不能反复看测试集调整模型？
speaker 2: 因为一旦你根据测试集结果反复调参，测试集实际上就变成了另一个验证集，就不再能客观反映模型在真正未见数据上的表现了。
speaker 1: 图里给的推荐比例是训练集 70%到80%，验证集和测试集各10%到15%，这个比例是固定的吗？
speaker 2: 不是绝对固定的，会根据数据总量和任务调整，但这是一个比较常见、合理的起点。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
