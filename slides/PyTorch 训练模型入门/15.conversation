speaker 1: 前面几章分别讲了回归、多分类、二分类各自的损失函数，能不能总结一下怎么选？
speaker 2: 可以，图里按任务类型分了几类：普通回归用 MSELoss，多分类用 CrossEntropyLoss，二分类和多标签用 BCEWithLogitsLoss。
speaker 1: 如果数据里有一些明显的异常值，MSELoss 还合适吗？
speaker 2: 不一定，因为 MSE 是平方误差，异常值会被放大惩罚，这时候可以考虑 L1Loss 或者 SmoothL1Loss，对异常值更稳健。
speaker 1: 多标签分类是什么意思，和多分类有什么区别？
speaker 2: 多分类是一个样本只属于一个类别，多标签是一个样本可能同时属于多个类别，这时候每个类别相当于一个独立的二分类问题，所以也用 BCEWithLogitsLoss。
speaker 1: 语言模型用的是 token 级别的 CrossEntropy，这是什么意思？
speaker 2: 语言模型是在每一个位置都要预测下一个 token，所以损失是针对每个位置分别计算交叉熵，再综合起来。
speaker 1: 图里还提到损失函数不等于业务指标，这句话怎么理解？
speaker 2: 意思是我们训练时优化的是损失函数，比如 CrossEntropy，但业务上真正关心的可能是准确率、F1 这些指标。
speaker 1: 为什么不能直接把准确率当损失函数来优化？
speaker 2: 因为梯度下降要求损失函数是连续、几乎处处可导的，而准确率这种指标本身是离散的、不可导，没法直接用来算梯度。
speaker 1: 所以训练和评估其实是两套体系，只是有关联？
speaker 2: 对，训练看损失函数下降，最终效果还是要看业务指标，两者关联但不完全等价。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
