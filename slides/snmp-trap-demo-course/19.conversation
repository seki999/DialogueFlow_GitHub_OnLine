speaker 1: 前面理论讲了很多，能不能实际跑一遍看看效果？
speaker 2: 可以，我们按顺序发送三种事件，先看 normal。命令是 docker compose run --rm iot-simulator normal。
speaker 1: 这条对应字典里的哪一行？
speaker 2: 对应通知 OID .0.1，Trap 名是 normalTrap，eventType 是 NORMAL，severity 是 INFO，这些值应该会一致地出现在数据库和页面上。
speaker 1: 接下来试试温度过高的场景？
speaker 2: 命令是把参数换成 temperature-high，这次对应 .0.2，Trap 名 temperatureHighTrap，eventType 是 TEMPERATURE_HIGH，severity 变成 WARNING，级别明显更高了。
speaker 1: 最后是设备错误？
speaker 2: 参数换成 device-error，对应 .0.3，Trap 名 deviceErrorTrap，eventType 是 DEVICE_ERROR，severity 是 CRITICAL，这是三种里级别最高的。
speaker 1: 发完这三条之后去哪里看结果？
speaker 2: 打开浏览器访问 localhost:8080/traps，应该能看到三行新记录，按接收时间从新到旧排列，最新发的 device-error 一般会排在最上面。
speaker 1: 如果我想单独确认某个 Trap 名和 OID 的对应关系对不对，有什么办法？
speaker 2: 可以用 README 里给的 snmptranslate 命令，比如查 deviceErrorTrap，正确结果应该解析回 .1.3.6.1.4.1.99999.0.3，跟设计文档里的映射表完全对得上。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
