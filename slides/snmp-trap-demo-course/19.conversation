speaker 1: 标准输入的原始格式具体长什么样？
speaker 2: 第一行是受信元主机，第二行是受信元地址，从第三行开始，每一行是“数值 OID 加一个空格加值”，比如 .1.3.6.1.4.1.99999.1.1 双引号包着的 iot-001。
speaker 1: 脚本第一步做什么？
speaker 2: 先把前两行记下来，当作这次 Trap 的来源日志，方便后续排查是哪个地址发来的。
speaker 1: 然后怎么处理剩下的行？
speaker 2: 对每一行，按第一个空白字符把它拆成 OID 和值两部分。这里要注意是“第一个”空白，因为值本身可能包含空格，比如消息内容。
speaker 1: 拆完之后还要不要清理？
speaker 2: 要清理两处：OID 开头那个点号要去掉，值两侧的双引号也要去掉，这样才能拿到干净、可以直接比较和使用的数据。
speaker 1: 怎么知道哪一行是通知 OID，而不是普通的字段？
speaker 2: 有一行专门是 snmpTrapOID.0，它会被单独提取出来当作这次 Trap 的通知 OID，这一步是后面所有辞书查找的起点。
speaker 1: 这整个提取过程会不会受到 MIB 显示配置的影响？
speaker 2: 不会，因为 snmptrapd 从一开始启动就带了 -On 参数，输出的全部是数值形式的 OID，不依赖任何符号名解析或者本地化设置，所以这一步非常稳定。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
