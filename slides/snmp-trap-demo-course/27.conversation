speaker 1: 如果我发了一条 Trap，结果页面上没看到新记录，应该从哪里开始查？
speaker 2: 建议倒着走一遍数据流。先确认是不是只是页面没刷新，前面讲过浏览器不会自动推送新数据，刷新一下再看。
speaker 1: 刷新了还是没有呢？
speaker 2: 接着看 Oracle 里到底有没有这条记录，如果数据库里也没有，说明问题出在写入阶段，往前查 Spring Boot 的日志，用 docker compose logs monitoring-app 看有没有 400 或者 500 错误。
speaker 1: 如果 Spring Boot 那边什么日志都没有呢？
speaker 2: 那说明请求可能根本没到 Spring Boot，接着往前查 snmptrapd 和转发脚本的日志，用 docker compose logs snmptrapd，看有没有“Trap名变换失败”或者“整合性エラー”这类信息。
speaker 1: 如果连 snmptrapd 的日志都没有反应呢？
speaker 2: 那就要往最底层查了，确认 UDP 162 端口是不是真的通、Community 是不是配置成了 public、Simulator 的环境变量是不是指向了正确的主机。
speaker 1: 如果怀疑是辞书或者 MIB 本身写错了，有什么快速验证的方法？
speaker 2: 可以单独跑一次 validate-dictionary.sh 做构建时校验，或者直接用 snmptranslate 命令去解析某个 Trap 名，看它算出来的数值 OID 跟辞书里写的是不是一致。
speaker 1: 如果查了半天查不出头绪，想干脆重新来一遍呢？
speaker 2: 可以先执行 docker compose down -v，把容器和数据卷都清干净，再重新执行 docker compose up -d --build，从一个干净的环境重新验证。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
