speaker 1: 如果收到一个从没见过的通知 OID，脚本会怎么处理？
speaker 2: 它不会临时给这个 OID 起一个叫 unknownTrap 的通用名字然后照样转发，而是直接判定“Trap名变换失败”，把这条信息输出到标准错误，然后以退出码 1 结束，不会继续往下走。
speaker 1: 如果 OID 是已知的，是不是就可以直接转发了？
speaker 2: 还不行，查到 Trap 名之后还有第二关：要比较这次收到的 eventType 和 severity，跟辞书里为这个 Trap 预先定好的期望值是不是一致。
speaker 1: 如果不一致会怎样？
speaker 2: 脚本会把这种不一致当成两种可能：数据被篡改，或者发送端的实现跟约定不一致。不管是哪种，都拒绝转发，这一步同样会终止在“整合性エラー”，也就是一致性错误。
speaker 1: 为什么要这么严格，直接拒绝而不是打个警告日志然后照常发？
speaker 2: 因为这条数据一旦真的被写进数据库、显示在页面上，运维人员看到的就是一个自相矛盾的记录，比如 OID 说是设备错误但严重程度显示成正常。严格拒绝可以避免这种误导性的数据进入系统。
speaker 1: 所以只有两关都通过，才会走到最后生成 JSON 那一步？
speaker 2: 对，通知 OID 必须已注册，业务字段必须和期望值完全一致，两个条件同时满足，才会生成包含 trapOid 和 trapName 的 JSON，发给 Spring Boot。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
