speaker 1: 把这些环境变量放在一起看，能不能按组件分分类？
speaker 2: 可以分成三组。第一组是 Simulator 用的：SNMP_HOST、SNMP_PORT、SNMP_COMMUNITY，默认值分别是 snmptrapd、162、public，决定 Trap 往哪发。
speaker 1: 第二组呢？
speaker 2: 第二组是转发脚本用的：TRAP_API_URL 默认指向 http://monitoring-app:8080/api/traps，另外 TRAP_DICTIONARY 和 VARBIND_DICTIONARY 分别指向两份辞书文件的默认路径。
speaker 1: 第三组是数据库连接相关的吧？
speaker 2: 对，SPRING_DATASOURCE_URL、SPRING_DATASOURCE_USERNAME、SPRING_DATASOURCE_PASSWORD，默认连接本机 1521 端口的 FREEPDB1，用户名 snmpuser，密码 snmppassword。
speaker 1: 如果我想改一下发送目标地址，需要改代码吗？
speaker 2: 不需要，这些值都写在 docker-compose.yml 里，直接调整对应的环境变量就行，代码本身不用动。
speaker 1: 那这些默认的用户名密码能直接用在正式环境吗？
speaker 2: 不能，文档反复强调这些是演示专用的固定凭据，正式环境要通过 Secrets 管理之类的方式替换掉，不能原样沿用。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
