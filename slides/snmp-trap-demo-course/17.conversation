speaker 1: 为什么不干脆把所有规则都写在一个文件里，非要分成三层？
speaker 2: 因为这三层各自变化的频率和责任都不一样。MIB 管的是协议层的 OID 结构和符号名，变动最少；两份 TSV 辞书管的是运行时的业务规则，相对更容易调整。
speaker 1: 具体来说，脚本运行的时候怎么用到这三层？
speaker 2: 脚本先靠 VB 定义辞书把每个字段的 OID 转成 JSON 属性名，再靠 Trap 定义辞书把通知 OID 转成 Trap 名和期望的业务枚举值，MIB 本身在运行时主要用于构建时的交叉验证，而不是每次请求都去读。
speaker 1: 把规则写进两份 TSV，而不是硬编码进脚本，有什么好处？
speaker 2: 好处是改业务规则的时候，只需要改辞书文件，不用碰脚本代码逻辑本身，降低了改动范围，也降低了引入 bug 的风险。
speaker 1: MIB、辞书、脚本是分开部署的吗？
speaker 2: 不是，它们被一起打包进同一个容器镜像。这样做是为了让 Git 里的一次改动，和最终发布出去的一个镜像版本对应起来，不会出现代码更新了但辞书忘了更新的错位情况。
speaker 1: 那两个环境变量 TRAP_DICTIONARY 和 VARBIND_DICTIONARY 是干什么用的？
speaker 2: 它们可以指定辞书文件路径，但设计上只是给测试场景用的，不支持在生产运行时动态热切换辞书内容。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
