speaker 1: 这四个组件要跑起来，是不是要分别一个个启动？
speaker 2: 不需要，项目用 Docker Compose 把它们编排到一起，一条 docker compose up -d --build 就能把常驻服务全部构建并启动。
speaker 1: 哪些算常驻服务？
speaker 2: snmptrapd、monitoring-app、oracle-db 这三个是持续运行的；iot-simulator 不一样，它是一次性任务，要用 docker compose run --rm iot-simulator 加场景名才会触发一次发送。
speaker 1: 这些容器之间怎么知道对方在哪、怎么连接？
speaker 2: 靠环境变量，比如 SNMP_HOST、SNMP_PORT、SNMP_COMMUNITY 告诉 Simulator 该往哪发；TRAP_API_URL 告诉 snmptrapd 里的转发脚本该往哪投递 JSON；SPRING_DATASOURCE 系列告诉 Spring Boot 怎么连 Oracle。
speaker 1: 启动之前有什么要注意的吗？
speaker 2: 要确保宿主机的 UDP 162、TCP 8080、TCP 1521 这三个端口没被别的程序占用，不然会启动失败或者端口冲突。
speaker 1: 启动之后是不是立刻就能用了？
speaker 2: Oracle 首次启动一般要等几分钟做初始化，这是正常现象，不用担心，等它就绪之后整套系统才算真正可用。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
