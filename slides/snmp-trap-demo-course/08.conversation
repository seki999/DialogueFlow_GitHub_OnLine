speaker 1: 上一章说规则拆成了字典，那具体先看哪个字典？
speaker 2: 先看 trap-definitions.tsv，这个文件是通知级别的翻译表，决定一个 OID 最终变成什么 Trap 名。
speaker 1: 它每一行都存了什么？
speaker 2: 四列：通知 OID、Trap 名、期望的 eventType、期望的 severity。
speaker 1: 能举个具体例子吗？
speaker 2: 比如 .0.2 这一行，对应 Trap 名是 temperatureHighTrap，期望 eventType 是 TEMPERATURE_HIGH，期望 severity 是 WARNING。
speaker 1: 那这个'期望值'是干嘛用的，只是给人看的注释吗？
speaker 2: 不只是注释，它会在后面的处理流程里被拿来跟 Trap 报文里实际携带的 eventType、severity 做核对，如果对不上就说明数据有问题。
speaker 1: 如果字典里不小心写重复了同一个 OID 会怎样？
speaker 2: 那属于违反了唯一性要求，属于字典格式错误，正常情况下应该在构建阶段的校验脚本里就被挡下来，不会进入运行环境。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
