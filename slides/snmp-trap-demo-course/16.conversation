speaker 1: VB 定义辞书跟前面讲的 Trap 定义辞书是什么关系？
speaker 2: 两者分工不同。Trap 定义辞书管的是“这个通知 OID 对应哪个事件”，VB 定义辞书管的是“每一个字段 OID 对应哪个 JSON 属性、什么类型、多长”。
speaker 1: 它的列结构是怎样的？
speaker 2: 依次是 oid、symbol、jsonField、required、valueType、maxLength。比如 1.3.6.1.4.1.99999.1.5|temperature|temperature|true|number|32，这一行定义了温度字段。
speaker 1: valueType 有几种取值？
speaker 2: 三种：string、number、datetime。number 允许带符号的整数或小数，datetime 要求满足 ISO-8601 本地日期时间的前缀格式。
speaker 1: 如果收到一个辞书里没有定义的 OID 会怎么处理？
speaker 2: 会被直接忽略，不会报错也不会中断处理。这是为了保留 SNMP 协议本身的可扩展性，未来加新字段时旧版本脚本不会因为看不懂就崩掉。
speaker 1: 但如果是已知的必须字段呢？
speaker 2: 如果一个已知的必须字段最终解析出来是空值，整条 Trap 会被拒绝，不会带着缺失字段继续往下走。
speaker 1: 目前 required 这一列的值都是一样的吗？
speaker 2: 对，现有的 7 个字段都是 true，也就是全部必填，没有可选字段。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
