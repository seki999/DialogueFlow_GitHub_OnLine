speaker 1: 假设未来要新增一个第四种事件类型，第一步应该做什么？
speaker 2: 第一步是确认新的 OID 编号没有跟任何已有或者已废弃的 OID 冲突。规则很明确：已经在用的 OID，语义和类型不能改；如果某个事件废弃了，也不能把它的编号拿去复用，必须采一个全新的号。
speaker 1: 确认好新编号之后呢？
speaker 2: 第二步更新 MIB 里的符号和 OID 定义，第三步更新 Simulator，让它能生成带有新通知 OID 和新 VB 的 Trap。
speaker 1: 辞书那边是不是也要跟着改？
speaker 2: 对，第四步更新 Trap 定义辞书或者 VB 定义辞书。这里有个细节：只有涉及全新的 JSON 字段时，才需要同时改动转发脚本和 Spring Boot 代码；如果只是新增一个事件但复用已有字段，脚本和 Spring Boot 不用动。
speaker 1: 文档还有没有？
speaker 2: 有，第五步是同步更新 OID 一览表、转换表和测试规格，这几份文档要在同一次变更里一起更新，不能代码改了文档没跟上。
speaker 1: 最后怎么验证改得对不对？
speaker 2: 第六步按顺序跑验证：先用 snmptranslate 确认 MIB 解析正确，再检查 shell 脚本语法，再跑 Maven 测试，最后跑 Compose 端到端测试。文档还给了一个具体例子，验证 temperatureHighTrap 应该解析出 .1.3.6.1.4.1.99999.0.2，跟预期值一致才算通过。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
