speaker 1: 整门课学下来，能不能用一句话概括这套系统在做什么？
speaker 2: 可以：设备通过 SNMP Trap 主动上报事件，经过接收、转换、校验之后，被持久化到数据库，最终在网页上以列表形式展示出来，一共六个环节，缺一不可。
speaker 1: 整门课里，你觉得最重要的一条设计思路是什么？
speaker 2: 是“分层校验、逐层拒绝”。从 Trap 名转换的 OID 校验，到业务字段一致性校验，到 API 层的必填和长度校验，再到数据库层的非空约束，每一层都不允许把有问题的数据传给下一层。
speaker 1: 辞书体系那部分，回过头看，你觉得设计的巧妙之处在哪？
speaker 2: 巧妙之处在于把“协议规则”和“业务规则”拆开管理：MIB 管协议层的 OID 结构，两份 TSV 辞书管业务层的语义映射，再用构建时校验把两者绑定在一起，改动业务规则不需要碰脚本代码，也不会出现代码和辞书各说各话的问题。
speaker 1: 这门课反复强调项目的边界，比如不支持 SNMPv3、不支持自动重试，这是不是也是一种学习方法？
speaker 2: 是的，明确知道一个系统“没做什么”，和知道它“做了什么”同样重要。这能帮我们准确判断这套演示适合用来学习什么，不适合直接照搬到什么场景。
speaker 1: 如果还想继续往下学，你会建议往哪些方向深入？
speaker 2: 可以研究一下 SNMPv3 的 authPriv 认证机制，看看它是怎么解决 Community 明文这个短板的；也可以了解消息队列在保证可靠投递方面能起到什么作用；还可以学习怎么给这种多容器系统设计自动化的端到端测试。
speaker 1: 好，这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。这也是整门课的最后一章，接下来完成本章的五道综合测验，再回顾一下前面各章的重点，就完整地过了一遍这套系统了。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
