speaker 1: 如果发过来的数据本身有问题，比如字段缺失，系统是怎么处理的？
speaker 2: 要看是在哪个环节出问题。如果是通知 OID 根本没在字典里登记，脚本层面就会直接终止，连 HTTP 请求都不会发出去。
speaker 1: 那必填字段缺失呢，也是脚本层面拦？
speaker 2: 对，还有类型不对、超长、eventType 和字典期望值不一致，这几种都会在 forward-trap.sh 这一层被拦下来，本质上都不会到达 Spring Boot。
speaker 1: 那如果脚本这边都通过了，是不是就一定能存进数据库？
speaker 2: 不一定，Spring Boot 那边还有独立的一道校验，就是 Bean Validation，万一请求体依然缺字段，会返回 400。
speaker 1: 那 500 是什么情况才会出现？
speaker 2: 500 是请求本身完全合法，但数据库那边出了问题，比如连接异常导致保存失败，这跟前面的"数据本身有问题"是两码事，属于基础设施故障。
speaker 1: 为什么要搞两层校验，而不是干脆都放在一个地方做？
speaker 2: 因为两层面对的输入来源不一样，脚本层管的是原始 SNMP 报文的可信度，API 层管的是 HTTP 请求本身的合法性；就算以后有别的调用方绕开脚本直接打 API，Bean Validation 依然能兜底，这是一种纵深防御的思路。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
