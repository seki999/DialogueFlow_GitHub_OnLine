speaker 1: trap-definitions.tsv 是通知级别的，那字段级别的映射在哪个文件？
speaker 2: 在 varbind-definitions.tsv，它负责把每一个具体的 VB OID 翻译成 JSON 字段。
speaker 1: 这个文件每行存了哪些信息？
speaker 2: 五列：VB OID、对应的 JSON 字段名、是否必填、类型、最大长度。
speaker 1: 举个例子，比如温度这个字段是怎么定义的？
speaker 2: 温度对应的 OID 是 1.3.6.1.4.1.99999.1.5，JSON 字段名是 temperature，类型标记为十进制数字字符串，最终会被解析成数字。
speaker 1: 一共定义了多少个字段？
speaker 2: 七个：deviceId、deviceName、eventType、severity、temperature、message、eventTime，覆盖了 Trap 事件需要的全部业务信息。
speaker 1: 这些必填和长度限制是摆设吗，还是真的会拿来校验？
speaker 2: 是真校验，转发脚本会拿着这些规则去检查实际收到的每个字段，如果必填字段是空的，或者超过最大长度，就会中止转发，不生成 JSON。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
