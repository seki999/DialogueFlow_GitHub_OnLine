speaker 1: 之前提到forward-trap.sh转发失败会存进队列，具体是怎么落盘的？
speaker 2: 它不会直接写最终的文件名，而是先写一个以点开头的临时文件，写完之后再用mv把它改名成eventId.json。
speaker 1: 为什么要多这一步？
speaker 2: 因为mv在同一个文件系统内是原子操作，这样能保证退出队列目录看到的每一个文件都是完整写好的，不会读到写了一半的半截数据。
speaker 1: 那谁负责把队列里的数据重新发出去？
speaker 2: 有一个独立运行的后台脚本retry-queue.sh，它是个死循环，默认每5秒扫描一次队列目录，间隔可以用环境变量调整。
speaker 1: 如果同时跑了两个retry-queue.sh实例，会不会抢着处理同一个文件？
speaker 2: 不会，它先把文件mv改名加上.processing后缀，这个mv本身是原子的，谁先抢到谁处理，另一个实例发现文件已经不在了，直接跳过。
speaker 1: 重试失败了怎么办？
speaker 2: 就把.processing文件改名换回原来的名字，留在队列里，等下一轮循环继续尝试，直到API恢复正常为止。
speaker 1: 万一同一条数据被重复投递了好几次，数据库里会不会出现好几条重复记录？
speaker 2: 不会，因为Oracle表对eventId设置了唯一约束，再加上TrapService保存之前会先查一遍是否已经存在，两层保障让重复投递变成安全的幂等操作。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
