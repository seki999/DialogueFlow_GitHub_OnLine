speaker 1: 我们刚才看了整体架构，从 Simulator 开始，它具体是怎么工作的？
speaker 2: Simulator 是用 Java 21 写的命令行程序，底层用 SNMP4J 这个库来构造和发送 SNMP v2c 的 Trap 报文。
speaker 1: 它发送的每条 Trap 里包含哪些数据？
speaker 2: 对应到代码里的 TrapEvent 类，一共 7 个字段：deviceId、deviceName、eventType、severity、temperature、message、eventTime。
speaker 1: 那我要发一条'设备错误'的 Trap，具体怎么触发？
speaker 2: 在命令行加个参数就行，比如 device-error，Simulator 内部已经预置了 normal、temperature-high、device-error 三种场景。
speaker 1: 它怎么知道要发到哪里去？
speaker 2: 靠三个环境变量：SNMP_HOST、SNMP_PORT、SNMP_COMMUNITY，Compose 里默认分别是 snmptrapd、162、public。
speaker 1: 如果我想测试发到另一台机器，是不是要改代码？
speaker 2: 不需要，直接改这几个环境变量就行，这也是把配置和代码分离的一个好处。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
