speaker 1: 我们一直在说 OID，比如 .0.2 代表温度过高，这些数字到底是怎么分配出来的？
speaker 2: 项目用了一个私有的 Enterprise Root，就是 1.3.6.1.4.1.99999，所有自定义的东西都挂在这个根下面。
speaker 1: 根下面又分了哪些分支？
speaker 2: 分两支，一支是 demoNotifications，放通知类的 OID，比如三种 Trap；另一支是 demoObjects，放数据字段的 OID，比如 deviceId、temperature。
speaker 1: 那命名上有什么讲究吗？我看到 SNMP-TRAP-DEMO-MIB 全是大写，但 temperatureHighTrap 又是小写开头的？
speaker 2: 对，这是故意区分的，MIB 模块名用大写加连字符，具体的 OBJECT-TYPE 和 NOTIFICATION-TYPE 用小驼峰命名，这样一眼就能分清是模块还是具体对象。
speaker 1: 如果以后不想要某个 Trap 类型了，能不能把它的编号删掉给新的用？
speaker 2: 不行，规则很明确，已经发布的 OID 语义和类型不能改，要废弃也不能复用编号，只能分配一个全新的号码，这是为了避免历史数据和新语义混在一起造成误判。
speaker 1: 为什么项目不直接用业界标准的 MIB，比如 SNMPv2-SMI？
speaker 2: 因为 Debian 精简版的 Net-SNMP 默认不自带这些标准 MIB，它们属于另外的许可分发范畴，所以项目干脆做了一个自包含的 MIB，只靠 OBJECT IDENTIFIER 就能完成名字解析，不依赖外部标准包。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
