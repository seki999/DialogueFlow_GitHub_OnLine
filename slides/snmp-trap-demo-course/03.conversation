speaker 1: 上一章讲了SNMPv3的原理，这一章能不能画个整体的路线图，让我知道数据是怎么走的？
speaker 2: 可以。最左边是Java Simulator，它模拟设备用SNMPv3 authPriv发Inform，走UDP 162端口。
speaker 1: 发出去之后先到哪里？
speaker 2: 先到UDP Gateway，也就是nginx的stream模块，它会用一致性哈希把请求分发给receiver-a或者receiver-b。
speaker 1: 为什么是两个接收节点，不是一个？
speaker 2: 为了高可用。这两个节点各自独立运行，任何一个出故障，另一个仍然能继续接收，不会影响整体服务。
speaker 1: 接收节点收到Trap之后做什么？
speaker 2: 它们会把原始的OID和VB转换成结构化数据，如果转发失败，就先存进共享的持久化重试队列，成功的话就用Bearer Token把JSON发给HTTP Gateway。
speaker 1: HTTP Gateway和UDP Gateway是同一个东西吗？
speaker 2: 是同一个nginx，只是它同时开了两块能力：stream模块处理UDP 162，http模块处理8080端口，对Web端做负载均衡。
speaker 1: 后面接的是什么？
speaker 2: 两个Spring Boot实例，monitoring-app-a和monitoring-app-b，它们都连接同一个Oracle FREEPDB1数据库，保证数据一致。
speaker 1: 运维想看告警列表的时候怎么进来？
speaker 2: 通过浏览器访问同一个HTTP Gateway，用Basic Auth登录，生产环境上还会走HTTPS。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
