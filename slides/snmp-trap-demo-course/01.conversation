speaker 1: 这个项目到底是用来解决什么问题的？
speaker 2: 简单说，就是演示当一个设备状态发生变化时，怎么把这个'事件'从设备一路送到运维人员能看到的页面上。
speaker 1: 那为什么不直接让管理端定时去查设备状态就行了，比如每分钟查一次？
speaker 2: 那种方式叫轮询，问题是如果设备在两次查询之间出现了异常，你要等到下一次轮询才能发现，反应会有延迟。
speaker 1: 所以 Trap 就是相反的思路，设备自己主动通知？
speaker 2: 对，设备一旦状态变化就立刻发送一个 Trap 报文，不需要管理端来问，这样几乎是实时的。
speaker 1: 那这个 Demo 具体包含哪些部分？
speaker 2: 一共四个角色：负责模拟设备发 Trap 的 IoT Simulator，负责接收并转换报文的 snmptrapd，负责保存和展示的 Spring Boot 应用，还有负责持久化的 Oracle 数据库。
speaker 1: 听起来已经挺完整了，那是不是可以直接拿去生产环境用？
speaker 2: 不行，项目 README 里写得很清楚，这只是 SNMP v2c 的演示，没有加密认证，也没有多节点部署和失败重试队列，这些都被明确排除在范围之外。
speaker 1: 了解，那我们接下来是不是要一步步看这四个角色具体是怎么工作的？
speaker 2: 没错，下一章我们先补一下 SNMP Trap 的核心概念，把 Poll 和 Trap 的区别、OID 这些术语讲清楚，再往下拆解每个组件。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
