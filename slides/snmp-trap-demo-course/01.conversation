speaker 1: 我们要学的这个项目，标题里写着“生产部署”，它到底是为了解决什么问题？
speaker 2: 简单说，就是很多团队一开始用最基础的snmptrapd，收到告警只是打印一行日志，运维要靠人工盯屏幕或者翻日志文件。
speaker 1: 那如果日志文件被自动清理或者滚动删除了呢？
speaker 2: 历史告警就没了，出问题以后想复盘“上周三凌晨到底发生了什么”，基本无从查起。
speaker 1: 除了查询问题，还有什么隐患？
speaker 2: 比如后端处理服务突然宕机几分钟，这段时间内到达的告警，如果没有落盘机制，就直接丢失了。
speaker 1: 那安全性呢？谁都能往这个端口发消息吗？
speaker 2: 传统方案里确实缺少认证，理论上任何机器都可以伪造一条Trap发过来，系统没法分辨真假。
speaker 1: 所以这门课要讲的项目，就是把这几个短板都补上？
speaker 2: 对，它引入了SNMPv3认证、把Trap转换成结构化JSON、用持久化队列做失败重试，还把数据存进Oracle数据库，配上一个Web页面查看。
speaker 1: 这样一来，运维就不用死盯着终端了。
speaker 2: 没错，而且历史数据可以随时查询，重复的告警也会被自动去重，不会重复入库。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
