speaker 1: forward-trap.sh 发出去的 JSON 最终敲的是哪个门？
speaker 2: 是 Spring Boot 提供的 POST /api/traps 接口，Content-Type 固定是 application/json。
speaker 1: 这个接口对请求体有什么要求？
speaker 2: 要求携带 9 个必填字段：trapOid、trapName、deviceId、deviceName、eventType、severity、temperature、message、eventTime，每个字段都有对应的类型和长度限制。
speaker 1: 如果这些字段里少了一个会怎样？
speaker 2: 会触发 Bean Validation 校验失败，接口返回 400 Bad Request，告诉调用方请求本身有问题。
speaker 1: 如果字段都齐全，保存的时候数据库挂了呢？
speaker 2: 那种情况属于服务端内部错误，返回 500 Internal Server Error，跟 400 的"客户端问题"是两回事。
speaker 1: 那正常成功的情况呢？
speaker 2: 数据成功写入之后返回 201 Created，表示这条记录已经被成功创建。
speaker 1: eventTime 这种时间字段有什么格式要求？
speaker 2: 要求 ISO-8601 本地日期时间格式，这样前后端、数据库对时间的理解才能保持一致，不会因为格式歧义出问题。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
