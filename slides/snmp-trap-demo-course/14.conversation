speaker 1: 上一章讲了TrapService怎么处理数据，这一章我们看看数据最终是怎么存进Oracle的？
speaker 2: 靠TrapEvent这个JPA Entity，它映射到Oracle里的SNMP_TRAP_EVENT表，每个字段都对应表里的一列。
speaker 1: 主键是怎么生成的？
speaker 2: 用的是GenerationType.IDENTITY策略，对应数据库里的GENERATED BY DEFAULT AS IDENTITY，主键完全交给Oracle自己生成，Java代码不用操心。
speaker 1: 我看代码里有个protected修饰的无参构造函数，这是干嘛用的？
speaker 2: 这是JPA规范的要求，框架从数据库读出一条记录、要反射创建对应的Java对象时，必须有这样一个无参构造函数，但用protected修饰可以防止业务代码不小心直接调用它。
speaker 1: Entity上写的length和数据库表里VARCHAR2的长度是一回事吗？
speaker 2: 概念上要保持一致，但因为项目配置的是ddl-auto: validate，表结构实际是Flyway脚本控制的，Entity上的length更多是文档说明，加上validate阶段的核对作用。
speaker 1: 数据库里还专门建了一个索引，是做什么用的？
speaker 2: IDX_SNMP_TRAP_RECEIVED_TIME，专门给按接收时间倒序查询用的，因为Web页面展示列表时用的正是这种排序方式，有了索引才能保证数据量大了以后依然查得快。
speaker 1: EVENT_ID的唯一约束是从一开始就有的吗？
speaker 2: 不是，V1版本创建表的时候还没有EVENT_ID这一列，是V2迁移脚本才加进来的，还专门给旧数据回填了一个legacy格式的eventId，之后才添加唯一约束，这也说明了这个平台是逐步演进出去重能力的。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
