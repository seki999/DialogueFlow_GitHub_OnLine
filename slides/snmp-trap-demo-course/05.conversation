speaker 1: Simulator 发出去的 Trap，第一个接住它的是谁？
speaker 2: 是 snmptrapd，它是 Net-SNMP 自带的一个接收服务，监听所有网卡的 UDP 162 端口。
speaker 1: 它怎么判断这条报文能不能处理？
speaker 2: 靠 Community 字段，配置里写的是 authCommunity log,execute,net public，意思是只有 Community 为 public 的报文才被允许记录日志、触发处理器。
speaker 1: 如果报文的 Community 不是 public 会怎样？
speaker 2: 会被直接丢弃，不会进入后续处理流程，这就是它最基础的一层过滤。
speaker 1: 通过认证之后呢，是不是每种 Trap 走不同的处理逻辑？
speaker 2: 不是，配置里用的是 traphandle default，意思是不管什么类型的通知，统一都转交给同一个脚本 forward-trap.sh 去处理。
speaker 1: 那启动参数里那些 -f -Lo -On 是干嘛的？
speaker 2: -f 是前台运行方便容器管理，-Lo 是把日志打到标准输出，-On 是让 OID 以数字形式显示，这样后面脚本比较的时候不用管符号名或者本地化设置。
speaker 1: 那 snmptrapd 会不会自己把数据存到数据库？
speaker 2: 完全不会，它的职责很纯粹，只管接收、认证、转交，存储、业务查询这些都不归它管，都交给后面的 Spring Boot。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
