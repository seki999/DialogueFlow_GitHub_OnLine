speaker 1: 上一章讲了模拟器怎么发消息，那消息到了UDP Gateway之后是怎么被分配到两个接收节点的？
speaker 2: 靠一致性哈希，nginx配置里写着hash $remote_addr$remote_port consistent，把来源IP和端口组合起来算哈希值。
speaker 1: 为什么不能简单地轮询分发，让两个节点负载更均衡？
speaker 2: 因为SNMPv3有个特殊要求，discovery阶段协商的Engine ID和USM安全状态，必须在后续的Inform请求里保持一致。
speaker 1: 也就是说同一台设备的请求必须固定走同一个节点？
speaker 2: 对，如果这次请求分到receiver-a，下次又分到receiver-b，receiver-b并不知道之前协商过的安全上下文，认证就会失败。
speaker 1: 那如果以后要加第三个接收节点，会不会把所有映射关系都打乱？
speaker 2: 不会大规模打乱，这正是consistent一致性哈希的优势，只有哈希环上相邻的一小部分映射会变化，大部分设备还是继续路由到原来的节点。
speaker 1: 配置里还有个reuseport，这个是干什么的？
speaker 2: 它允许多个nginx worker进程同时监听UDP 162端口，内核帮忙分发连接，可以提升整体的并发处理能力。
speaker 1: 这两个机制是配合使用的吗？
speaker 2: 是的，reuseport解决并发吞吐问题，一致性哈希解决路由稳定性问题，两者一起保证了UDP层既能扩展又不会破坏SNMPv3的安全会话。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
