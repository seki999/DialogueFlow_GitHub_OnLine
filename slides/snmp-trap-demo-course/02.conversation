speaker 1: 轮询和 Trap 这两种方式，到底有什么本质区别？
speaker 2: 轮询是管理端主动去问设备“你现在怎么样”，设备被动回答。Trap 反过来，是设备主动说“我这里发生了一件事”。
speaker 1: 那这个项目里用的是哪一种？
speaker 2: 用的是 Trap。IoT Simulator 模拟设备，一旦触发某个事件，就主动发送一条 Trap 消息，不需要管理端先来问。
speaker 1: Trap 消息是靠什么协议传输的？
speaker 2: 是 UDP。UDP 的特点是轻量、无连接，但也没有送达确认。
speaker 1: 没有送达确认听起来有点risky，万一丢了怎么办？
speaker 2: 这正是设计文档里特别强调的一点：发送端“发送成功”只代表消息离开了发送端，并不代表接收端真的收到了。
speaker 1: 那怎么知道消息到底有没有被处理？
speaker 2: 只能通过下游各环节的日志和界面来确认，比如 snmptrapd 的日志、Spring Boot 的日志，以及最终网页上是否出现了这条记录。
speaker 1: 所以这也是为什么后面会强调，每一层都要能独立排查。
speaker 2: 对，因为 UDP 本身不提供保证，整个链路的可靠性要靠各环节的可观测性来弥补。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
