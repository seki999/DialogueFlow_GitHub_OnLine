speaker 1: 我们先聊聊SNMPv3吧，它和之前的版本比，安全性到底强在哪？
speaker 2: SNMPv3引入了三种安全级别：noAuthNoPriv完全不认证、authNoPriv只认证不加密、authPriv两者都做。
speaker 1: 那这个项目用的是哪一种？
speaker 2: 强制用最高级别authPriv，认证算法是SHA-256，加密算法是AES-128，这两者结合起来才叫authPriv。
speaker 1: 那如果有人试图用SNMPv2c的方式接入呢？
speaker 2: 会被拒绝。README里写得很清楚，不接受SNMPv2c和Community public，模拟器也只允许SNMP_VERSION=3。
speaker 1: 我一直分不清Trap和Inform，它们不是一回事吗？
speaker 2: 名字很像，但机制不同。Trap是单向的，设备发出去就不管了，不知道对方收没收到。
speaker 1: 那Inform呢？
speaker 2: Inform要求接收方必须回一个SNMP RESPONSE，而且errorStatus要是noError才算真正送达。
speaker 1: 所以Inform更可靠？
speaker 2: 对，这也是为什么IotSimulator默认用Inform——它可以准确知道通知到底送达没有，而不是发出去就当作成功。
speaker 1: 那什么时候会用Trap呢？
speaker 2: 项目也支持通过环境变量切换成Trap模式，但因为Trap本身没有送达确认，切换后只能靠接收日志、队列和数据库里的eventId去反推追踪。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
