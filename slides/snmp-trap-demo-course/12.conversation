speaker 1: 三种事件具体是怎么和 OID 对应起来的？
speaker 2: NORMAL 对应 1.3.6.1.4.1.99999.0.1，TEMPERATURE_HIGH 对应 .0.2，DEVICE_ERROR 对应 .0.3，末尾数字依次递增，一一对应。
speaker 1: 每个 OID 又对应一个 Trap 名，这个名字是怎么来的？
speaker 2: 是 MIB 里定义的符号名，采用小驼峰命名：normalTrap、temperatureHighTrap、deviceErrorTrap，三者严格一一对应，不会出现一个 OID 对应两个名字的情况。
speaker 1: 实际触发的时候命令行参数是怎么写的？
speaker 2: 分别是 normal、temperature-high、device-error，这三个参数直接决定了 Simulator 会构造哪一个 OID 的 Trap。
speaker 1: 除了名字，每个事件还绑定了严重程度吗？
speaker 2: 对，Trap 定义辞书里给每个事件都配了期望的 severity：NORMAL 是 INFO，TEMPERATURE_HIGH 是 WARNING，DEVICE_ERROR 是 CRITICAL。
speaker 1: 这个期望值有什么用？
speaker 2: 后面在转发脚本那一步会用它来做一致性校验，如果收到的实际 severity 跟这个期望值对不上，脚本会认为数据有问题，直接拒绝转发。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
