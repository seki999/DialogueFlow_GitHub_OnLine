speaker 1: snmptrapd 具体是什么软件？
speaker 2: 它是 Net-SNMP 套件里的 Trap 接收守护进程，这里用的是 5.9 系列、Debian bookworm 打包的版本。
speaker 1: 它监听哪里？
speaker 2: 监听全部 IPv4 接口的 UDP 162 端口，用的是 SNMP v2c，Community 是 public。
speaker 1: 启动参数里那几个字母是什么意思？
speaker 2: -f 表示前台运行，不脱离终端；-Lo 表示日志输出到标准输出，方便容器化环境查看；-On 表示用数值形式的 OID 而不是符号名。
speaker 1: 为什么一定要数值形式的 OID？
speaker 2: 因为后面转发脚本要拿这个 OID 去查字典做比较，数值形式不依赖 MIB 的显示配置和本地化设置，更稳定可靠。
speaker 1: 那 snmptrapd 自己会不会把数据存到数据库？
speaker 2: 不会，这正是它的边界。它只负责协议接收、加载 MIB 字典、提取 OID 和 VB、做 Trap 名称转换，数据库持久化是 Spring Boot 的事。
speaker 1: 它怎么把数据交给后面处理？
speaker 2: 靠一条配置 traphandle default，意思是所有收到的通知都交给 forward-trap.sh 这个脚本去处理，脚本再负责后续的转换和转发。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
