speaker 1: 如果我要排查一次失败的 Trap，应该按什么顺序去看各层的错误？
speaker 2: 可以按数据流的顺序，从 Simulator 层往下查。Simulator 层的规则很简单：参数不合法退出码 2，发送过程中出异常退出码 1，并且会打印原因。
speaker 1: 如果 Simulator 这边正常，那接着看哪一层？
speaker 2: 接着看转发脚本层。这一层如果发现必须的 OID 缺失，或者调用 API 失败没拿到 2xx 响应，都会把信息写到标准错误，然后退出码是 1。
speaker 1: 再往下就是 API 层了吧？
speaker 2: 对，API 层如果发现必须字段缺失，返回 HTTP 400；如果是数据库层面出了问题，返回 HTTP 500，两种情况都会留下相应的日志。
speaker 1: 这个系统有没有自动重试机制，比如失败了自动再发一次？
speaker 2: 没有，文档明确写了自动再送和隔离队列不在这个最小化演示的范围内。出错就是终止，不会自动重试，这也是这套系统作为“演示”而非“生产系统”的一个明显边界。
speaker 1: 把这几层规则放在一起看，有没有什么共性？
speaker 2: 有一个很明显的共性：每一层出错都会把问题挡在本层，不会把有问题的数据硬塞给下一层继续处理。这种“宁可拒绝也不将就”的思路，贯穿了整条链路。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
