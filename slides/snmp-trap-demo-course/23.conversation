speaker 1: 前面讲了很多业务逻辑，那这些逻辑是怎么被测试覆盖的？
speaker 2: 项目里既有单元测试，也有集成测试，分工是不一样的。先说单元测试，比如TrapEventRequestTest，专门测DTO上那些Pattern和Size注解在合法、非法输入下的表现。
speaker 1: TrapService的逻辑怎么测？没有真实数据库怎么办？
speaker 2: 用Mock，TrapServiceTest会Mock掉TrapEventRepository，让existsByEventId在不同用例下返回true或者false，这样就能单独验证save方法里判重的分支逻辑，完全不需要连接真实Oracle。
speaker 1: 那集成测试是做什么的？
speaker 2: TrapFlowIntegrationTest覆盖的是端到端的链路，从API请求进来，到数据真正被持久化，再到重复请求确实被正确去重，这是多个组件协作之后的整体行为验证。
speaker 1: 它跟TrapServiceTest的区别是什么？
speaker 2: TrapServiceTest只隔离测试Service这一层，靠Mock跳过了数据库；集成测试则是把真实的组件串起来一起跑，验证的是整体协作的效果，两者是互补关系，不是重复的。
speaker 1: iot-simulator那边也有自己的测试吗？
speaker 2: 有，TrapEventTest测的是Simulator这一侧构造事件模板和相关字段的逻辑，跟monitoring-app的测试是完全独立维护的两套代码，因为这本来就是两个不同的Maven项目。
speaker 1: 运行这些Java测试对本地环境有要求吗？
speaker 2: 文档提供了用官方Maven镜像跑容器化测试的方式，把项目目录挂进容器里执行mvn test，这样即使本地没装Maven或者JDK版本不一致，也能得到一致的测试结果。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
