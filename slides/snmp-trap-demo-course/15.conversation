speaker 1: 数据最后存进数据库的表长什么样？
speaker 2: 表名是 SNMP_TRAP_EVENT，由 JPA 的 ddl-auto=update 自动创建，不需要手写建表语句。
speaker 1: 主键是怎么设计的？
speaker 2: ID 字段，类型是 Long，用 IDENTITY 自增策略，也就是每插入一条记录，数据库自动给它分配一个递增的 ID。
speaker 1: 我看到有 TRAP_OID 和 TRAP_NAME 两个字段，都是必填吗？
speaker 2: 对，都是 NOT NULL，限制 128 字符，这样能保证每条记录都能明确追溯到它到底是哪种通知。
speaker 1: 设备相关的字段呢？
speaker 2: DEVICE_ID 限制 64 字符，DEVICE_NAME 限制 128 字符，也都是必填，跟前面 API 那边的校验规则是一致的。
speaker 1: 我注意到有两个时间字段，EVENT_TIME 和 RECEIVED_TIME，有什么区别？
speaker 2: EVENT_TIME 是事件本身发生的时间，来自 Trap 报文；RECEIVED_TIME 是系统真正接收并保存这条数据的时间，两者可能会有一点点延迟差。
speaker 1: 这个区分有什么实际用处？
speaker 2: 比如你想知道设备最近一次上报是什么时候，就该按 RECEIVED_TIME 降序查；但如果你想还原当时事件的真实发生时刻，就得看 EVENT_TIME。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
