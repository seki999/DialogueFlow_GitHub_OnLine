speaker 1: trap-definitions.tsv 这个文件长什么样？
speaker 2: 是一个用竖线分隔列的文本文件，UTF-8 编码。每一行代表一个事件的定义，比如 1.3.6.1.4.1.99999.0.2|temperatureHighTrap|TEMPERATURE_HIGH|WARNING。
speaker 1: 列的顺序是固定的吗？
speaker 2: 固定，依次是 notificationOid、trapName、eventType、severity，脚本按位置读取，不能随意调换。
speaker 1: 这些列各自有什么校验规则？
speaker 2: notificationOid 必须是数字和点号组成、文件内唯一，并且要跟 MIB 里同一个 Trap 的 OID 完全一致；trapName 要小驼峰、唯一，还得真实存在于 MIB 中；eventType 要大写加下划线、唯一；severity 只能是 INFO、WARNING、CRITICAL 三选一。
speaker 1: 为什么强调“不能反过来用 eventType 推算 Trap 名”？
speaker 2: 因为如果按 eventType 去反推，一旦发送端的 eventType 字段被篡改或者写错，系统可能会把这条 Trap 误判成别的事件类型。而以通知 OID 作为唯一检索键，就避免了这种风险。
speaker 1: 所以这份辞书的核心作用，其实是把“协议里的数字”翻译成“业务上能看懂的名字和枚举值”。
speaker 2: 说得很准确，它是连接协议层数字世界和业务层语义世界的桥梁。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
