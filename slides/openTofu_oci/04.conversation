speaker 1: OpenTofu 怎么知道要先创建 VCN，再创建 Subnet，最后才创建 Compute 实例呢？是不是靠代码写的顺序？
speaker 2: 不完全是靠书写顺序，而是靠“引用表达式”。比如 Compute 模块里写 subnet_id = module.network.public_subnet_id，这个引用本身就告诉 OpenTofu：必须先有这个 Subnet 的输出值，才能填进 Compute 的配置里。
speaker 1: 原来如此，那这种引用关系是怎么变成实际的创建顺序的？
speaker 2: OpenTofu 会根据所有的引用表达式，在内部构建一张依赖图。图上标明了谁必须先于谁完成，然后按照这张图去调度创建、更新、删除的顺序。
speaker 1: 那 depends_on 是干什么用的？我看有些教程会让你到处加它。
speaker 2: depends_on 只应该用在“值引用表达不出来的隐藏依赖”上，这种情况其实很少见。比如两个资源之间没有任何属性引用关系，但业务上又确实要求一个先于另一个完成，这时候才需要手写 depends_on。
speaker 1: 那是不是每两个资源之间都应该保险起见加一个 depends_on？
speaker 2: 不建议这样做。大多数依赖关系已经能被引用表达式自然表达出来了，如果再手写一遍 depends_on，只是增加了额外的维护成本，而且容易掩盖真正的设计问题。
speaker 1: 如果依赖图上两个资源完全没有关系，会怎么处理？
speaker 2: 这种互相独立的资源可以被并行创建，这也是依赖图设计的一个好处，不需要严格串行地一个一个来，效率会更高。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。