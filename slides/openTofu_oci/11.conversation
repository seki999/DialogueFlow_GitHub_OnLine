speaker 1: 我一直好奇，点了 apply 之后，命令行敲完命令按下回车，接下来到底发生了什么？
speaker 2: 简单说，OpenTofu 会把这次要做的变更，通过 OCI API 发送出去，请求创建或更新对应的资源。因为 OCI 很多资源的准备工作是异步的，Provider 会一直等待，直到资源真正准备完成。
speaker 1: 那 State 文件是什么时候更新的？
speaker 2: 就在这个等待完成之后。OpenTofu 会把 OCI 返回的真实 OCID，以及各种属性值，写回到 State 文件里。这样下一次运行 plan 的时候，才有依据去计算差异。
speaker 1: 所以下一次 plan，其实是在比较什么？
speaker 2: 是在比较三者：你代码里写的期望状态，State 文件里记录的上一次结果，还有 OCI 上实际的当前状态。这三者放在一起对比，才能算出这次到底需要做哪些改动。
speaker 1: 那 destroy 呢，删除的顺序有讲究吗？
speaker 2: 有的。destroy 大致会按照依赖图的逆序来发出删除请求，也就是说，最后创建的资源，通常会最先被删除，这样才能避免删除一个还被其它资源依赖的东西。
speaker 1: 有没有可能删除会失败？
speaker 2: 完全可能。比如一个 Bucket 里还有 Object 没清空，就没法直接删除；一个 Volume 还挂在某个实例上，也删不掉；数据库如果正处在某种中间状态，也需要等它变成终态才能删除。
speaker 1: 那如果我在 OCI 控制台里手动加了一些东西，会不会影响 destroy？
speaker 2: 会有风险。因为这些手动添加的依赖不在代码和 State 的追踪范围内，OpenTofu 事先并不知道它们的存在，遇到这种情况往往需要人工介入，先手动处理掉这些外部依赖，才能顺利完成 destroy。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。