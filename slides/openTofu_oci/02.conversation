speaker 1: 为什么大家都说手动在控制台里点来点去不好，用代码不是更麻烦吗？
speaker 2: 表面上看好像点鼠标更快，但问题在于：你点了哪些步骤，只留在你自己的记忆里，别人很难复现，你自己过几个月也可能忘了具体顺序。
speaker 1: 那用代码写下来，是不是就能完全保证每次结果一模一样？
speaker 2: 不能完全保证。IaC 确实让期望状态变成了可以放进 Git 审查的代码，相同的输入更容易得到相似的结构，但账户配额、区域容量、Provider 版本、甚至有人在控制台手动改了东西，都可能让结果出现差异。
speaker 1: 所以 IaC 更像是提高确定性，而不是消除所有不确定性？
speaker 2: 说得很准确。它把“凭记忆操作”变成“凭代码审查”，这是质的提升，但不是魔法。
speaker 1: 图里那个从 State 指回 Plan 的箭头是什么意思？
speaker 2: 这说明 State 不是一次性用完就丢弃的。每次你运行 plan，OpenTofu 都会读取 State，把它和代码里的期望状态、以及 OCI 上的实际状态做对比，才能算出这次要做哪些改动。
speaker 1: 那是不是说，只要我写好了 .tf 文件，就不用管代码之外的东西了？
speaker 2: 不是的，工程师依然要持续关注凭证是否有效、配额够不够、区域容量是否充足。IaC 只是把可以代码化的部分变得透明和可审查，但云服务本身的限制依然存在。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。