speaker 1: 老师，我看代码里有个 count = var.enabled ? 1 : 0，这是什么意思？
speaker 2: 这是一个非常常用的开关写法。它的意思是：如果 enabled 这个变量是 true，就创建 1 份这个资源；如果是 false，就创建 0 份，也就是完全不创建。这样你只需要改一个变量的值，就能控制某个资源要不要存在。
speaker 1: 明白了，那 for_each = toset(["80", "443"]) 这种写法又是干嘛的？
speaker 2: 这个是用来批量创建同类资源的。比如你要给 NSG 开放 80 端口和 443 端口两条规则，用 for_each 遍历这个集合，就会按照每个稳定的 Key，也就是端口号，分别创建一条对应的规则。
speaker 1: 为什么要强调“稳定的 Key”？
speaker 2: 因为 for_each 是按 Key 来追踪资源身份的，如果 Key 不稳定，比如用列表的索引位置作为 Key，那么当列表顺序变化时，OpenTofu 可能会误以为资源变了，从而做出意料之外的删除重建。用端口号这种稳定值作为 Key 就安全得多。
speaker 1: 那条件表达式 condition ? true_value : false_value 呢，看起来跟很多编程语言里的三元运算符很像。
speaker 2: 对，就是同一个思路。根据条件是真是假，选择两个值当中的一个。前面那个开关写法里的 var.enabled ? 1 : 0，其实就是条件表达式的一个具体应用。
speaker 1: 那 dynamic 块又是用来做什么的？
speaker 2: dynamic 用来批量生成结构相同的嵌套配置块。比如 Service Gateway 可能需要根据一个可选的路由列表，动态生成多条 route_rules，如果用 dynamic，就不用为每一条路由都手写一遍重复的嵌套块。
speaker 1: 这几种语法是不是经常会组合在一起用？
speaker 2: 是的，非常常见。比如一个资源可能同时用 count 控制整体是否创建，内部又用 for_each 或 dynamic 来处理多条规则，这样既灵活又不至于让代码重复冗长。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。