speaker 1: 企业里的那些交换机、路由器是怎么和OCI这边的系统连起来的？
speaker 2: 通常是通过IPSec VPN或者FastConnect接入的，前者基于互联网隧道，搭建比较快；后者是专线级别的连接，一般更稳定。
speaker 1: 接进来之后是直接进VCN吗？
speaker 2: 中间会经过OCI的DRG，也就是动态路由网关，它是VCN和外部网络对接的核心枢纽，负责把外部路由和VCN内部路由对接起来。
speaker 1: 接通了是不是就万事大吉了？
speaker 2: 还不是。接通只是第一步，还得确认往返路由是不是都打通了，比如OKE这边能不能连到设备网段，设备网段的回包能不能顺利送回OKE，另外防火墙规则是否放行对应端口，这些都得逐一检查。
speaker 1: 为什么特别提到TCP Ping和SNMP GET需要关注这个？
speaker 2: 因为这两个是平台主动发起请求，还要等设备回包，如果路由只通了一半，或者防火墙只放行了单向，就会出现超时或者拿不到数据的情况，排查起来比较麻烦。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
