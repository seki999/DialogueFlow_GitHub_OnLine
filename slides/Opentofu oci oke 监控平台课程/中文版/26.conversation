speaker 1: 所有服务是不是都可以直接用CPU使用率来做HPA？
speaker 2: 不是。像monitoring-api、alert-engine这种无状态服务，CPU和请求量关系比较直接，用CPU做HPA是合适的。
speaker 1: 那Poller呢，它的负载看CPU准确吗？
speaker 2: 不太准确，Poller的负载更多体现在待处理的任务数量、设备数量，或者队列长度上，所以更适合用这些自定义指标去扩缩容，而不是单纯看CPU。
speaker 1: Trap和Syslog接收器呢？
speaker 2: 它们更适合根据每秒接收到的报文数量、队列积压情况，或者处理延迟来扩容，因为这些指标才真正反映了它们当前承受的压力。
speaker 1: 配置HPA的时候还有什么要注意的吗？
speaker 2: 资源请求和Probe一定要配置正确，因为应用启动期和Readiness状态会直接影响HPA用来计算的指标，如果Probe配置不当，可能导致Pod被错误地计入或排除在统计之外，进而让HPA做出错误的扩缩容判断。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
