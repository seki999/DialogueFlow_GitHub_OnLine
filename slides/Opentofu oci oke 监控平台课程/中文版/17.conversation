speaker 1: Syslog和前面讲的SNMP Trap是不是差不多？
speaker 2: 都是设备主动发消息过来，属于入站流量，但Syslog传输的是日志文本，而且支持的协议方式更丰富一些。
speaker 1: 具体有哪些方式？
speaker 2: 常见的有UDP 514这种传统方式，TCP 514提供更可靠的传输，还有TCP 6514是走TLS加密的，通常叫Syslog over TLS。
speaker 1: 应该怎么选择用哪种？
speaker 2: 如果是生产环境、比较看重安全性，优先用TLS 6514；如果是内部网络但需要可靠传输，可以用TCP 514；如果设备比较老只支持传统方式，那就只能兼容UDP 514了。
speaker 1: 解析这块有什么难点？
speaker 2: 难点在于既要兼容新的RFC 5424格式，也要兼容不少设备还在用的传统RFC 3164风格，另外不同厂商还可能有自己的私有格式，这些都需要在解析规则里处理。
speaker 1: 如果设备突然产生大量日志怎么办？
speaker 2: 这就要考虑背压处理了，比如设置合理的队列和限流策略，避免瞬间大量日志把内存打爆，也要考虑单条日志的最大长度限制，防止异常报文影响整体处理。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
