speaker 1: SNMP Trap一般是怎么发过来的？
speaker 2: 设备在检测到某些事件的时候，会主动把Trap报文发到监控平台的UDP 162端口，比如接口断开、设备重启这些事件。
speaker 1: UDP是不是不太可靠？
speaker 2: 对，UDP没有连接保证，可能会丢包、乱序，甚至因为网络重传出现重复报文，设备本身的时钟也不一定准确，这些都是Trap Receiver要面对的现实情况。
speaker 1: 那接收端要怎么设计才不会出问题？
speaker 2: 不能收到报文之后马上做很重的数据库操作，因为故障爆发时Trap可能瞬间大量涌入。推荐的做法是先快速解析出来源IP、OID这些基本字段，放进一个内部队列，然后立刻返回继续接收下一个报文，真正的标准化和写库放到异步流程里处理。
speaker 1: 那这个入口应该怎么对外暴露？
speaker 2: 因为它是UDP协议的入站流量，应该用OCI的Network Load Balancer来接收，而不是普通的HTTP负载均衡器，这样才能正确处理和保留这类流量。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
