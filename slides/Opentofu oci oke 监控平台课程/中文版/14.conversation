speaker 1: SNMP GET具体是怎么工作的？
speaker 2: 简单说，监控平台作为SNMP Manager，向设备（也就是SNMP Agent）发送一个GET请求，问它某个指标是多少，比如CPU使用率，设备收到后把具体数值返回来。
speaker 1: 怎么知道该查哪个指标？
speaker 2: 每个指标都有一个编号，叫OID，比如查询设备名称对应的是一个固定的OID。而OID具体代表什么含义，是写在MIB文件里的，相当于一份说明书。
speaker 1: SNMP的安全性是不是都一样？
speaker 2: 不一样。SNMPv2c只依赖一个叫Community的口令，安全性比较弱；如果条件允许，更推荐使用SNMPv3，它支持基于用户的身份认证和数据加密，安全性要高不少。
speaker 1: 所以选型上应该优先考虑哪个？
speaker 2: 如果设备支持，并且团队有能力管理SNMPv3的用户和密钥，优先用v3；如果设备比较老旧只支持v2c，那至少要严格管理好Community口令，避免泄露。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
