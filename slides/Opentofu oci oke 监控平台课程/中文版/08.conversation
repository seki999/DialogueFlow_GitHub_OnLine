speaker 1: 为什么VCN里要分这么多子网，不能就用一个大网段吗？
speaker 2: 如果所有资源都挤在一个子网里，后面想针对某一类资源单独设置安全规则或者路由，会非常困难，所以推荐按用途拆分。
speaker 1: 具体是怎么拆的？
speaker 2: 比如把Kubernetes API Endpoint、Worker Node、Pod、内部负载均衡、数据库，还有管理用的Bastion，各自放在不同的子网段里。
speaker 1: Pod子网为什么划得比Worker Node子网还大？
speaker 2: 因为一个Worker Node上通常会跑很多个Pod，Pod的数量比Node多得多，所以需要更大的地址空间来分配IP。
speaker 1: 创建OKE集群之前是不是必须先把这些网络资源都准备好？
speaker 2: 对，OKE依赖VCN、子网、路由表、安全列表或者NSG这些前置资源，OCI虽然也可以自动帮你创建网络，但正式项目通常还是自定义网络，这样才能精确控制路由和访问权限。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
