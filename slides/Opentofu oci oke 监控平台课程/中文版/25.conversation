speaker 1: 怎么保证一个服务在节点出问题的时候还能正常提供服务？
speaker 2: 首先重要服务的副本数至少要设成3个，然后配合Pod反亲和性和拓扑分布约束，让这些副本尽量分散到不同的节点上，避免一个节点挂了把所有副本都带走。
speaker 1: PodDisruptionBudget又是干什么的？
speaker 2: 它是用来在节点维护这种计划性中断的时候，限制同时能有多少个副本不可用，保证至少有一定数量的副本始终在线。
speaker 1: Spring Boot自己这边要配置什么吗？
speaker 2: 建议开启优雅关闭，也就是graceful shutdown，应用收到关闭信号之后，会先给正在处理的请求一点时间完成，而不是直接粗暴地中断连接。
speaker 1: 滚动更新的正确顺序应该是什么样的？
speaker 2: 新Pod先启动，等startupProbe通过，再等readinessProbe通过，负载均衡才开始把流量转发过去；等新Pod真正接手流量之后，旧Pod才会被标记为NotReady，完成手头剩余的请求，最后才真正关闭。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
