speaker 1: 如果SNMP GET的Poller也做了多副本部署，会不会出现重复轮询同一台设备的问题？
speaker 2: 确实会，如果没有协调机制，多个Poller副本可能会同时去查同一台设备，这是需要专门解决的问题。
speaker 1: 有什么办法可以避免？
speaker 2: 常见的有三种方案。第一种是用数据库的行锁，SQL里用FOR UPDATE SKIP LOCKED，一个Poller抢到某条任务后加锁，其他Poller查询时会自动跳过被锁定的行。
speaker 1: 第二种呢？
speaker 2: 第二种是按设备ID做哈希分片，比如hash(device_id) % pod_count，这样每台设备就固定由某一个Poller负责，天然就不会重复。
speaker 1: 第三种是什么？
speaker 2: 第三种是引入消息队列，由一个Scheduler统一把任务放进队列，多个Poller作为Consumer各自消费不同的任务，同样避免了重复处理。
speaker 1: 那是不是直接用Spring的@Scheduled定时任务就行了？
speaker 2: 在多副本场景下单纯用@Scheduled并不可靠，因为每个副本会各自独立触发，没有协调，很容易导致重复轮询，所以正式系统通常会选择数据库锁或者消息队列这类更可靠的方案。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
