speaker 1: 这套系统是用来监控别的设备的，那它自己出问题了怎么办？
speaker 2: 所以必须建立自监控体系，不能只盯着外部设备，自己也要被监控起来，一般会分成四个层面。
speaker 1: 哪四个层面？
speaker 2: 第一是Spring Boot和JVM层面，比如堆内存、GC情况、线程数、HTTP延迟、数据库连接池状态；第二是协议层面的指标，比如tcp_ping_total、snmp_trap_received_total、syslog_dropped_total这些。
speaker 1: 还有呢？
speaker 2: 第三是Kubernetes层面，像Pod重启次数、有没有Pending的Pod、有没有OOMKilled、节点是不是NotReady；第四是OCI层面，比如负载均衡器的健康状态、NLB的流量、数据库的CPU和存储空间使用情况。
speaker 1: 为什么要分这么细？
speaker 2: 因为如果监控平台自己的某个环节出了问题，比如队列积压导致大量Syslog被丢弃，却没有被发现，那外部设备真正出故障的时候，反而不会被及时告警，后果会很严重，所以每一层都要覆盖到。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
