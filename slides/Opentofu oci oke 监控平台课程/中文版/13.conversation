speaker 1: TCP Ping是不是就是普通的Ping命令？
speaker 2: 不是。普通Ping用的是ICMP协议，TCP Ping是尝试对目标IP的指定端口建立TCP连接，通过连接成功、被拒绝还是超时，来判断这个端口是不是可达。
speaker 1: 那实现的时候要注意什么？
speaker 2: 首先一定要设置连接超时，不然遇到无响应的设备，连接会一直挂着；其次要限制并发数量，不然可能把本地的临时端口耗尽，也可能对设备造成压力。
speaker 1: 每次探测都要记录哪些信息？
speaker 2: 至少要记录设备ID、IP、端口、这次连接耗时多久，还有具体是成功、被拒绝、超时，还是遇到了别的网络错误，比如无法路由到目标主机。
speaker 1: TCP Ping需要经过负载均衡器吗？
speaker 2: 不需要，它是平台主动向设备发起的出站连接，Poller直接连过去就行，不涉及入站流量，所以不需要NLB或者HTTP LB。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
