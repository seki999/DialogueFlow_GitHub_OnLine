speaker 1: 为什么要专门做一套系统去监控这些网络设备？
speaker 2: 因为企业里有大量的路由器、交换机和服务器，运维人员没法一台一台手动去看它们的状态，需要一个平台自动帮他们盯着。
speaker 1: 那平台是怎么知道设备状态的？
speaker 2: 主要靠四种通信方式：TCP Ping、SNMP GET、SNMP Trap 和 Syslog。
speaker 1: 这四种是不是都一样，都是平台去问设备？
speaker 2: 不一样。TCP Ping 和 SNMP GET 是平台主动去连设备、去查询，这叫出站流量；而 SNMP Trap 和 Syslog 是设备自己主动发消息过来，这叫入站流量。
speaker 1: 为什么要区分这两种方向？
speaker 2: 因为它们在网络设计上完全不同。出站的不需要负载均衡器，平台自己发起连接就行；入站的必须有一个稳定的地址接收，通常要用负载均衡器，而且是UDP/TCP类型的，不是普通的HTTP负载均衡器。
speaker 1: 所以这套系统其实不只是一个网站那么简单？
speaker 2: 对，它其实是一整套云原生系统，涉及基础设施代码、OCI网络、Kubernetes、四种协议的采集与接收、Spring Boot服务、还有Oracle数据库存储数据，后面每一章我们会逐个拆开讲。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
