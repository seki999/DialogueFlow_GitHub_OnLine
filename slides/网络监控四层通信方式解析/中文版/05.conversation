speaker 1: 这张决策图把响应分成 SYN-ACK、RST 和超时三类。
speaker 2: 是的，它们提供不同强度的证据，不能都归为简单的成功或失败。
speaker 1: 收到 SYN-ACK 就可以说端口开放。
speaker 2: 通常可以，同时还要保留检测来源和时间范围。
speaker 1: RST 为什么不等于主机完全不可达？
speaker 2: 因为 RST 本身就是目标或中间设备返回的明确响应，常见含义是端口关闭或拒绝。
speaker 1: 超时的解释最模糊。
speaker 2: 对，它可能是报文被丢弃、防火墙静默过滤、路由故障、目标宕机或返回路径问题。
speaker 1: 所以告警不能把所有超时直接写成服务器宕机。
speaker 2: 正确，应写成从当前监控点访问目标端口超时，再结合其他证据定位。
speaker 1: 图中端口开放之后还有应用层检测。
speaker 2: 因为 TCP 只确认连接，应用可能卡死、返回错误，或者依赖数据库不可用。
speaker 1: TCP 检测适合做快速可达性和延迟趋势。
speaker 2: 是的，但不适合独自承担完整健康判断。
speaker 1: 这一章的原则就是不要让证据说得比它实际知道的更多。
speaker 2: 这也是监控告警保持可信度的关键。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
