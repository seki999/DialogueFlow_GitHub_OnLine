speaker 1: 时间线展示了十秒故障，却在两次 GET 中都看到 up。
speaker 2: 对，因为轮询只采到 10 点整和 10 点 01 分的两个瞬间。
speaker 1: 把周期从一分钟改成一秒就能解决吗？
speaker 2: 会降低遗漏概率，但请求量、设备处理、网络流量和存储成本都会明显上升。
speaker 1: 所以周期是风险与成本的平衡。
speaker 2: 是的，关键接口可以更频繁，稳定且低价值的对象可以更慢。
speaker 1: 短时变化更适合由 Trap 通知。
speaker 2: 对，Trap 负责即时事件，GET 负责随后确认当前状态和持续趋势。
speaker 1: 如果 GET 超时，能判断是 Agent 挂了吗？
speaker 2: 不能单凭超时判断，也可能是路由、ACL、凭据、设备负载或返回报文丢失。
speaker 1: 轮询任务还需要限制重试和并发。
speaker 2: 没错，否则故障期间大量重试可能进一步压垮设备或监控平台。
speaker 1: 图里的例子解释了采样盲区。
speaker 2: 它也说明历史曲线没有异常，不代表期间绝对没有发生过异常。
speaker 1: 合理方案是分级周期、Trap 补充和容量监控。
speaker 2: 这三者一起让轮询既及时，又不会变成新的负担。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
