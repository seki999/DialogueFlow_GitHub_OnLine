speaker 1: 这张选择图把需求先分成四种证据。
speaker 2: 对，不要先问哪个协议最好，而要先问当前缺少哪类证据。
speaker 1: 可达性对应 TCP Ping，目标端口就是被测业务端口。
speaker 2: 它主要产出成功、失败和延迟，但诊断深度有限。
speaker 1: 当前指标对应 SNMP GET，典型端口是 UDP 161。
speaker 2: OID 数据结构化，适合保存历史、计算速率和绘制趋势。
speaker 1: 即时事件对应 Trap，典型接收端口是 UDP 162。
speaker 2: 它实时性高，但普通 Trap 可能丢失，也不能持续提供全部指标。
speaker 1: 详细上下文对应 Syslog，常见 514 或 TLS 6514。
speaker 2: 它擅长原因分析和审计，但格式、解析和日志量更复杂。
speaker 1: 表格中的端口都应该理解为典型值。
speaker 2: 正确，生产环境的映射、TLS 终止和厂商配置可能不同。
speaker 1: 趋势图最适合 GET，故障原因更依赖 Syslog。
speaker 2: 而快速发现变化依赖 Trap，外部影响验证依赖 TCP 探测。
speaker 1: 四种证据的方向、时效和可信边界都不同。
speaker 2: 统一对比的目的就是让它们在同一事件中各司其职。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
