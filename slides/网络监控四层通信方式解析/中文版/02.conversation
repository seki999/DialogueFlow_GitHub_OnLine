speaker 1: 图中监控服务器向设备发出两条箭头，又接收两条箭头，核心区别是什么？
speaker 2: 向外的 TCP 检测和 SNMP GET 是主动探测，向内的 Trap 和 Syslog 是被动接收。
speaker 1: 主动探测的优势是监控端可以控制时间吗？
speaker 2: 对。它可以按固定周期执行，失败后也能按策略重试。
speaker 1: 被动消息的优势就是事件发生后更快到达？
speaker 2: 通常如此。设备不必等到下一轮轮询就能报告变化。
speaker 1: 但如果设备断电，可能连消息都发不出来。
speaker 2: 这正是被动接收的盲区，主动探测可以从外部发现设备已经不可达。
speaker 1: 反过来，只有主动轮询也会漏掉两次轮询之间的短故障。
speaker 2: 没错，所以即时通知和定期确认需要组合。
speaker 1: 图里看起来只有服务器和设备，实际系统还需要哪些部分？
speaker 2: 至少需要探测调度器、Trap 和日志接收器、历史存储、规则判断与事件关联。
speaker 1: 主动和被动不是健康与故障的简单对应。
speaker 2: 对。它们描述的是谁先发起通信，而不是消息一定代表正常还是异常。
speaker 1: 后面分析故障时，也要先看每条证据是主动取得还是被动收到。
speaker 2: 这样就能理解证据的时效性、可重试性和可能缺失的原因。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
