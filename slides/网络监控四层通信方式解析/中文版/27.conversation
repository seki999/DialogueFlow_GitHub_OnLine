speaker 1: 可靠监控图先把注意力转向监控管道本身。
speaker 2: 如果接收器失效，设备再正常发送也不会形成有效告警。
speaker 1: 接收链路自检包括端口、进程、队列和解析。
speaker 2: 还应定期发送测试 Trap，验证端到端路径而不只是进程存活。
speaker 1: 统一 NTP 时间为什么单独成为一条主线？
speaker 2: 因为交换机、防火墙和服务器时间不一致时，故障先后顺序会被扭曲。
speaker 1: 时间同步本身也要监控偏差。
speaker 2: 对，配置了 NTP 不等于时钟当前一定准确。
speaker 1: 安全传输分支列出 SNMPv3 和 TLS。
speaker 2: 它们分别加强 SNMP 与日志链路的身份、完整性或机密性。
speaker 1: ACL 和防火墙白名单仍然需要。
speaker 2: 协议安全与网络边界应叠加，同时限制允许发起监控的源地址。
speaker 1: 账户采用只读与最小权限。
speaker 2: 并且要安全保存、轮换凭据和证书，避免把秘密写入课程或日志。
speaker 1: 可靠性、时间一致性和安全性共同决定证据是否可信。
speaker 2: 这三项是生产化设计中不能被协议功能替代的基础。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
