speaker 1: 风险图把 Trap 问题分成丢失、风暴和上下文不足。
speaker 2: 这三类分别影响事件是否到达、平台是否扛得住以及事件是否足够解释。
speaker 1: 丢失不只发生在网络中吧？
speaker 2: 对，也可能发生在套接字缓冲区、接收队列、解析器或后续消息管道。
speaker 1: 图给出的对策是测试接收链路。
speaker 2: 可以定期发送测试 Trap，并监控端口、进程、队列和解析失败数。
speaker 1: 告警风暴通常来自同一故障触发大量关联接口事件。
speaker 2: 是的，需要按设备、接口、事件类型和时间窗口去重、抑制或限流。
speaker 1: 但抑制过头也可能隐藏真正的新故障。
speaker 2: 所以规则要保留首个事件、计数、持续时间和状态变化，而不是简单丢弃。
speaker 1: 上下文不足时就触发 GET。
speaker 2: GET 用来确认当前状态，Syslog 则补充时间线和可能原因。
speaker 1: Trap 只能收到设备愿意并被配置上报的事件。
speaker 2: 正确，未配置、Agent 故障或设备断电都可能导致没有消息。
speaker 1: 因此“没有 Trap”绝不能直接解释成“没有故障”。
speaker 2: 这条原则会在最后的监控设计章节再次进入完整闭环。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
