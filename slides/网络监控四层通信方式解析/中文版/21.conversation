speaker 1: 同一个接口断开，图里出现四种不同结果。
speaker 2: 这正好说明四种方式观察的是同一故障的不同侧面。
speaker 1: TCP 管理端口仍成功，为什么不矛盾？
speaker 2: 因为被测的是交换机管理面的 22 或 443，不一定是发生故障的业务接口。
speaker 1: GET 显示管理状态 up、运行状态 down。
speaker 2: 这表示管理员没有关闭接口，但接口实际链路已经断开。
speaker 1: Trap 的 linkDown 能更快通知接口变化。
speaker 2: 是的，变量中常带接口索引，平台需要把索引映射成可读接口。
speaker 1: Syslog 会提供接口名称和线路协议变化。
speaker 2: 它可能比 Trap 更详细，并帮助建立状态变化的先后顺序。
speaker 1: 如果只看 TCP，就可能误判整台交换机正常。
speaker 2: 准确说法只能是管理端口仍可连接，不能扩大成所有接口和业务正常。
speaker 1: 如果只看 Trap，也无法持续确认接口现在是否已经恢复。
speaker 2: 所以要立即用 GET 复核，并用 TCP 检查相关业务的影响范围。
speaker 1: 四条证据拼起来，状态、时间和影响才完整。
speaker 2: 对，这就是多信号关联的实际价值。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
