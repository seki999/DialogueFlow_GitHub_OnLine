speaker 1: 最后一张图把四种证据重新汇合到关联事件。
speaker 2: TCP 发现影响，GET 确认事实，Trap 快速触发，Syslog 分析原因。
speaker 1: 它们最终都服务于告警、处置和恢复验证。
speaker 2: 对，监控的目标不是收集越多越好，而是形成可行动的闭环。
speaker 1: 我再复述一次：TCP 成功不能直接等于应用健康。
speaker 2: 正确，它只证明从特定来源到特定端口在当时可以建立 TCP 连接。
speaker 1: GET 能给趋势，但可能漏掉采样间隙中的短故障。
speaker 2: 所以用 Trap 抢时间，再用 GET 复核当前状态。
speaker 1: Trap 没到也不能证明没故障。
speaker 2: 要主动监控接收链路，并用 TCP 和周期 GET 提供外部证据。
speaker 1: Syslog 最擅长解释，但要先解决时间和解析一致性。
speaker 2: 还要根据风险选择 TLS、访问控制和合理的保留策略。
speaker 1: 第一个延伸问题：如果设备量增加十倍，应该先评估什么？
speaker 2: 评估轮询并发、设备负载、网络流量、接收队列、解析吞吐和存储容量。
speaker 1: 第二个问题：怎样判断告警降噪没有隐藏真实故障？
speaker 2: 保留原始事件、计数和状态转换，用回放测试验证去重与抑制规则。
speaker 1: 第三个问题：四种方式之外，怎样更接近用户体验？
speaker 2: 增加协议、应用和端到端业务事务探测，并把结果与基础设施信号关联。
speaker 1: 第四个问题：修复完成的证据是什么？
speaker 2: 不仅要看到恢复消息，还要重新执行 GET、TCP 与业务检查，确认状态、影响和依赖都已恢复。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
