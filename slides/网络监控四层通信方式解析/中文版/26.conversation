speaker 1: 这张图把健康检查从网络一直推进到依赖关系。
speaker 2: 对，每向下一层，结论更接近用户真正体验的业务结果。
speaker 1: 网络可达之后才是 TCP 端口可连接。
speaker 2: 路由可达不保证端口开放，端口开放也不保证协议交互成功。
speaker 1: 比如 TCP 443 成功，但 TLS 握手可能失败。
speaker 2: 是的，TLS 成功后 HTTP 还可能返回错误。
speaker 1: HTTP 健康接口成功，也不一定证明下单事务成功。
speaker 2: 正确，业务流程可能依赖数据库、消息队列或第三方服务。
speaker 1: 最下面的依赖正常似乎又比单个事务更广。
speaker 2: 它提醒我们某次事务成功不代表所有依赖都具有足够容量和冗余。
speaker 1: 每层告警文案也必须准确。
speaker 2: TCP 失败应写端口不可连接，不应直接写整个应用宕机。
speaker 1: 这样团队知道下一步该检查哪一层。
speaker 2: 也能避免监控系统长期产生夸大或误导性的告警。
speaker 1: 四种通信方式主要覆盖其中若干层和侧面。
speaker 2: 对，完整业务可观测性还需要协议与事务级检查。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
