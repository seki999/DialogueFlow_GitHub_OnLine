speaker 1: 图里的 Manager 和 Agent 分别在哪一侧？
speaker 2: Manager 在监控系统侧，Agent 在交换机、服务器或设施设备侧。
speaker 1: Manager 的第一条箭头是查询请求。
speaker 2: 它负责安排轮询、发送请求、保存结果并根据规则判断是否告警。
speaker 1: Agent 会先从系统与硬件读取信息，再返回数据。
speaker 2: 对，Agent 是监控系统与设备内部状态之间的代理层。
speaker 1: 是不是设备内部有什么，Manager 就一定能读到什么？
speaker 2: 不是。只能读取设备和 Agent 实际暴露并授权访问的管理对象。
speaker 1: GET 的方向是 Manager 发起，Agent 响应。
speaker 2: 正确，这是主动轮询路径。
speaker 1: 图中 Agent 还有一条主动通知到 Trap 接收端。
speaker 2: 这说明同一个设备代理既可以响应查询，也可以主动上报事件。
speaker 1: Trap 接收端可以和 Manager 是同一套平台吗？
speaker 2: 可以是同一平台的不同组件，也可以分开部署后再汇总。
speaker 1: 理解角色之后，GET 和 Trap 的方向就不容易混淆了。
speaker 2: 对，下一步需要理解双方用什么结构来标识每个指标。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
