speaker 1: 传输图分成 UDP 514、TCP 和 TLS 6514。
speaker 2: 这些是常见方式和典型端口，实际部署仍要以双方配置为准。
speaker 1: UDP 的优势是轻量。
speaker 2: 对，但它没有连接和交付确认，拥塞或队列满时消息可能丢失。
speaker 1: TCP 有连接，是不是就绝对不会丢日志？
speaker 2: 不能这样保证。连接中断、发送缓存、应用崩溃和接收处理失败仍会造成缺口。
speaker 1: 图中特别写了正确分帧。
speaker 2: 因为 TCP 是字节流，双方必须用一致规则识别每条日志的边界。
speaker 1: TLS 分支增加了加密和身份校验。
speaker 2: 它适合保护敏感日志，生产环境通常应优先考虑。
speaker 1: TLS 的代价包括证书和连接管理。
speaker 2: 还包括握手、加密开销、证书轮换和失败排查。
speaker 1: 选择传输方式要看安全、可靠性、设备支持和日志量。
speaker 2: 对，同时还要监控丢包、连接状态、队列积压和接收吞吐。
speaker 1: 典型端口不能直接当成所有设备的固定事实。
speaker 2: 正确，防火墙与端口映射必须按实际配置验证。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
