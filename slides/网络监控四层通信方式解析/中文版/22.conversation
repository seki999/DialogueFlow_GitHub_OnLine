speaker 1: 盲区图中的四条线都以“只用”开头。
speaker 2: 因为问题不在工具本身，而在让单一工具承担超出边界的任务。
speaker 1: 只用 TCP 看不到设备内部指标。
speaker 2: 端口能连时，CPU 可能过高、风扇可能故障，某个业务接口也可能断开。
speaker 1: 只用 GET 会漏过采样间隙中的短时异常。
speaker 2: 对，而且轮询结果通常不能独自给出详细原因。
speaker 1: 只用 Trap 的风险是丢失和缺乏趋势。
speaker 2: 设备断电甚至可能来不及发送，因此沉默不能证明健康。
speaker 1: 只用 Syslog 也不能持续提供精确性能值。
speaker 2: 日志格式不统一、可能丢失，而且设备不一定记录所有状态。
speaker 1: 所以监控空白不能简单解释为正常。
speaker 2: 必须区分没有异常证据和已经取得正常证据。
speaker 1: 四种方式组合就是让一个信号补另一个信号的盲区。
speaker 2: TCP 验证外部可达，GET 读取事实，Trap 抢时间，Syslog 补上下文。
speaker 1: 这也解释了为什么生产监控需要证据关联。
speaker 2: 对，单一状态灯很简单，但很难支持可靠诊断。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
