speaker 1: 图中 ICMP 只得到“主机可能在线”，为什么结论这么保守？
speaker 2: 因为 ICMP 回应只证明这类网络消息可往返，不能证明某个业务进程正在监听。
speaker 1: TCP 443 的箭头更接近用户访问 HTTPS 服务。
speaker 2: 是的，它会经过路由、防火墙、目标操作系统和 443 端口监听这些环节。
speaker 1: 哪些环境会让普通 ping 不好用？
speaker 2: 云安全组、企业防火墙、只开放业务端口的网络，以及某些负载均衡场景都可能限制 ICMP。
speaker 1: 所以 TCP Ping 是为了绕过 ICMP 限制？
speaker 2: 不只是绕过限制，它还把探测目标对准了实际业务端口。
speaker 1: 如果 TCP 443 成功，就能宣布网站健康吗？
speaker 2: 不能。图的最后一步提醒我们，还要继续做 TLS、HTTP、应用和业务层检查。
speaker 1: 那 TCP 失败又能直接认定服务器宕机吗？
speaker 2: 也不能。失败可能来自路由、ACL、防火墙、端口未监听或超时等不同原因。
speaker 1: 这一章是在定义 TCP 检测证据的边界。
speaker 2: 对。它比 ICMP 更贴近端口，但仍只是分层健康检查中的一层。
speaker 1: 正确说法应是“从这个监控点到 TCP 443 当前可以建连”。
speaker 2: 这个表述准确地保留了来源、目标、端口和检测时刻。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
