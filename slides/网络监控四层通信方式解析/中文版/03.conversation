speaker 1: SNMP GET 具体是怎么工作的？
speaker 2: 它是监控服务器主动发起查询，通过 UDP 161 端口向设备的 SNMP Agent 发送 GET 请求。
speaker 1: 服务器怎么知道要查哪个指标？
speaker 2: 每个指标对应一个叫 OID 的编号，比如某个 OID 专门表示设备的运行时间。
speaker 1: 那可以查哪些内容？
speaker 2: 常见的有 CPU 使用率、内存占用、接口流量、接口状态，还有设备型号和运行时长。
speaker 1: 这种方式是不是要一直不停地查？
speaker 2: 对，它属于轮询模式，监控系统会按固定周期反复执行 GET 请求。
speaker 1: 轮询频率是不是越高越好？
speaker 2: 不是。频率太高会增加设备和网络负担，一般需要根据指标重要程度设置合理的采集间隔。
speaker 1: 版本上有什么区别吗？
speaker 2: SNMPv1 和 v2c 相对简单，但安全性较弱，SNMPv3 支持认证和加密，正式生产环境通常推荐使用。
