speaker 1: 图中 MIB 先连接到对象定义，说明它不是一堆实时数值。
speaker 2: 对。MIB 更像数据字典，定义有哪些对象、它们代表什么以及如何访问。
speaker 1: 对象定义下面有名称、数据类型、访问权限和 OID。
speaker 2: 这些信息让管理端知道该查询什么，也知道如何解释返回值。
speaker 1: OID 是不是指标的地址？
speaker 2: 可以这样类比。它是在层级树中的数字标识，用来定位某个管理对象或实例。
speaker 1: 图里的“具体指标实例”是什么意思？
speaker 2: 例如接口表中的某个接口会带索引，OID 加上索引后定位到该接口的具体状态。
speaker 1: MIB 和监控平台的时序数据库有什么区别？
speaker 2: MIB 定义语义，时序数据库保存每次采集得到的历史数值。
speaker 1: 不同厂商可以加入自己的对象吗？
speaker 2: 可以，私有 MIB 常用于描述设备特有的电源、风扇、温度和告警状态。
speaker 1: 如果没有正确 MIB，数字 OID 仍可能查询，但很难理解。
speaker 2: 正是如此，解析名称、类型和枚举值通常依赖正确的 MIB 定义。
speaker 1: 所以采集前要先建立对象与业务含义的映射。
speaker 2: 这样得到的指标才具有可读名称、单位和告警语义。
speaker 1: 这一章的主要内容已经讲完了吗？
speaker 2: 已经讲完了。接下来可以完成本章末尾的五道选择题，检查一下自己是否真正理解了这些内容。
