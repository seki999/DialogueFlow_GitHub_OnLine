speaker 1: 交换内存这个话题听起来有点底层，和 Kubernetes 有什么关系？
speaker 2: 节点上如果开启了交换内存，能在物理内存紧张时提供一层缓冲，但这也会影响调度器对资源的判断，所以 Kubernetes 对怎么配置交换内存有专门的指导。
speaker 1: 开启交换内存是不是没什么坏处？
speaker 2: 不是，交换会带来额外的性能开销，而且传统上 Kubernetes 对交换内存的支持比较保守，需要谨慎评估之后再决定是否启用。
speaker 1: 独立模式的 kubelet 又是什么意思？
speaker 2: 正常情况下 kubelet 要连接控制面的 API Server，接受调度指令；独立模式下它完全脱离这个连接，只负责管理本机上手动指定的容器。
speaker 1: 什么场景会用到这种独立模式？
speaker 2: 比较典型的是边缘设备或者一些特殊的本地环境，那里可能压根没有一个完整的控制面，只需要 kubelet 单独把容器管理起来。
