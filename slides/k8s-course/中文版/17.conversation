speaker 1: ZooKeeper 和前面的 Cassandra 都是分布式系统，运行方式有什么不一样？
speaker 2: 最大的不同是 ZooKeeper 对"多数派"这个概念特别敏感，必须保证任何时候都有过半数的节点存活，否则整个系统就不可用。
speaker 1: 那部署的时候要特别注意什么？
speaker 2: 首先节点数量通常是奇数，比如三个或五个，这样多数派的计算才没有歧义。
speaker 1: 除了数量，还有什么手段保证可用性？
speaker 2: 会用到 PodDisruptionBudget，限制集群维护时最多能同时驱逐多少个 ZooKeeper 节点，避免一次操作就打掉了多数派。
speaker 1: 节点会不会都挤在同一台物理机上，一台机器坏了就全军覆没？
speaker 2: 这个案例还用了反亲和性规则，尽量让不同的 ZooKeeper 节点被调度到不同的物理机器上，降低单点故障的影响范围。
