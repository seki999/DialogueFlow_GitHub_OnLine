speaker 1: 一提到 Kubernetes 集群，脑子里就是一堆机器，具体是怎么分工的？
speaker 2: 集群分成两部分，一部分叫控制面，负责做决策；另一部分是工作节点，负责真正运行你的容器。
speaker 1: 控制面里面具体有哪些角色？
speaker 2: 主要是 API Server，它是整个系统的统一入口，所有操作都要经过它；还有调度器，负责决定新创建的 Pod 该放到哪个节点上；以及控制器管理器，负责持续把集群调整成你期望的状态。
speaker 1: 那工作节点这边呢？
speaker 2: 每个节点上都有一个叫 kubelet 的组件，它接收控制面下发的指令，负责在本机启动、监控、停止容器。
speaker 1: 学习阶段是不是要自己搭一整套这么复杂的系统？
speaker 2: 不需要，学习环境通常用 Minikube，它能在你自己的电脑上，用一个虚拟机或容器模拟出一个五脏俱全的小集群，方便练习。
