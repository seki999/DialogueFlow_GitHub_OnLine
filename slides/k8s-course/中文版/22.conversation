speaker 1: 除了 Pod 安全标准，AppArmor 又是用来做什么的？
speaker 2: AppArmor 是 Linux 内核自带的一种强制访问控制机制，可以针对某个程序定义一份行为边界，规定它能做什么、不能做什么。
speaker 1: 用在容器上具体能限制哪些方面？
speaker 2: 常见的包括限制能访问哪些文件路径、能不能有某些特权网络操作、能调用哪些系统调用等等。
speaker 1: 要在 Kubernetes 里用起来，需要做什么准备？
speaker 2: 首先要在运行容器的节点操作系统上，提前加载好对应的 AppArmor 配置文件，Kubernetes 本身不负责生成这个文件。
speaker 1: 之后 Pod 怎么使用这份配置文件？
speaker 2: 在 Pod 的定义里通过注解或者专门的字段，指明这个容器要套用哪个 AppArmor 配置文件，这样即使容器内部进程被攻破，也很难越过这道边界去做更危险的事情。
