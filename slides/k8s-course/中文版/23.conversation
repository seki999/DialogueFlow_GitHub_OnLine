speaker 1: 刚讲完 AppArmor，Seccomp 又是从哪个角度做限制的？
speaker 2: Seccomp 专门限制容器进程能够调用哪些系统调用，也就是它能跟操作系统内核直接对话的范围。
speaker 1: 为什么要限制系统调用？
speaker 2: 因为绝大多数应用实际只用得到一小部分系统调用，如果不限制，一旦应用被攻破，攻击者理论上能滥用所有系统调用来搞破坏。
speaker 1: 这个配置是不是需要自己一个一个去写允许列表？
speaker 2: 不一定，Kubernetes 提供了一个内置的 RuntimeDefault 配置文件，作为一个比较合理、开箱即用的起点，能挡住大部分危险调用。
speaker 1: 如果想做得更精细呢？
speaker 2: 可以编写自定义的 Seccomp 配置文件，针对具体应用需要的系统调用做白名单，再配合前面讲的 AppArmor，形成多层次的防御体系。
