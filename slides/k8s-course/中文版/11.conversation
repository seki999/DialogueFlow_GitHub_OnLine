speaker 1: 一个 Pod 里放两个容器，是什么样的场景？
speaker 2: 典型场景是主容器跑业务逻辑，另外配一个 Sidecar 容器专门做辅助工作，比如收集日志、做流量代理。
speaker 1: 它们两个是怎么配合的？
speaker 2: 因为在同一个 Pod 里，它们天生共享网络命名空间，还可以共享同一份存储卷，所以 Sidecar 能很方便地拿到主容器的日志文件或者流量。
speaker 1: 为什么不把这些辅助功能直接写进主容器里？
speaker 2: 分开之后主容器可以只关心业务逻辑本身，升级、替换 Sidecar 也不会影响业务代码，职责更清晰。
speaker 1: Kubernetes 对 Sidecar 有专门的支持吗？
speaker 2: 有，较新版本支持把 Sidecar 声明为一种特殊的 Init 容器，让它能和主容器一起持续运行，而不是像普通 Init 容器那样跑完就退出。
