speaker 1: 前面学的都是单个组件，能不能有一个稍微完整一点的应用例子？
speaker 2: 有，官方教程里的留言板案例就很经典，用 Redis 做后端存储，前端是一个 Web 应用。
speaker 1: 这个案例里有几层组件？
speaker 2: 三层，前端负责接收用户提交的留言，Redis 主节点负责写入数据，Redis 副本节点负责分担读取压力。
speaker 1: 这几层是怎么互相找到对方的？
speaker 2: 每一层都有自己的 Deployment 和对应的 Service，前端通过 Service 的名字就能连接到 Redis 主节点，完全不用关心具体是哪个 Pod。
speaker 1: 副本节点是怎么和主节点同步数据的？
speaker 2: 副本节点启动后会主动连接主节点，做数据复制，这样即使主节点压力大，读请求也可以分流到副本上。
