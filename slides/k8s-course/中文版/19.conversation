speaker 1: 我的应用想根据访问者的真实 IP 做一些统计，结果发现拿到的都是集群内部的地址，这是怎么回事？
speaker 2: 这是 Service 默认转发行为导致的，为了实现跨节点的流量分发，请求经过节点转发时客户端的真实 IP 有可能被替换掉。
speaker 1: 那有办法保留真实的客户端 IP 吗？
speaker 2: 有，把 Service 的 externalTrafficPolicy 设置成 Local 就可以，这样流量只会被转发给本节点上的 Pod，源 IP 也就保留下来了。
speaker 1: 这个设置听起来只有好处，为什么不默认就这么用？
speaker 2: 因为用 Local 之后，流量不再跨节点均衡分发，如果某个节点上恰好没有这个应用的 Pod，那个节点收到的流量就没法处理，可能造成负载不均。
speaker 1: 那实际项目里应该怎么选？
speaker 2: 要看业务是不是真的依赖源 IP，比如做访问控制、限流、日志分析这些场景可能需要，如果不需要，默认的策略更简单也更均衡。
