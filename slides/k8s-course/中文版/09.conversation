speaker 1: 应用的配置信息，比如数据库地址，应该写死在镜像里吗？
speaker 2: 不建议，最好用 ConfigMap 把这些配置单独存起来，让镜像本身保持通用，不同环境用不同的 ConfigMap。
speaker 1: ConfigMap 里的内容，应用怎么拿到？
speaker 2: 有两种常见方式，一种是作为环境变量注入到容器里，应用直接读环境变量；另一种是作为文件挂载到容器的某个目录，应用去读文件内容。
speaker 1: 这两种方式怎么选？
speaker 2: 简单的键值对配置适合用环境变量，如果是配置文件这种结构化内容，挂载成文件更自然。
speaker 1: 如果我改了 ConfigMap 里的值，运行中的应用会立刻感知到吗？
speaker 2: 一般不会自动生效，多数情况下需要重启对应的 Pod，应用重新读取配置之后新值才会真正起作用。
