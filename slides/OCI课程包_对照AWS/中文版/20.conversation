speaker 1: 前面说安全控制这块跟AWS不完全一样,具体是怎么回事?
speaker 2: OCI提供了两种机制:安全列表和网络安全组,简称NSG。
speaker 1: 这两个有什么区别?
speaker 2: 安全列表是挂在子网上的,子网里所有实例共享同一套规则,思路上有点像早期网络里的ACL概念。
speaker 1: 那NSG呢?
speaker 2: NSG是挂在实例的网卡上,规则跟着实例走,这个更接近你熟悉的AWS安全组。
speaker 1: 两者是二选一,还是可以一起用?
speaker 2: 可以叠加使用,流量必须同时满足安全列表和NSG的规则才会被放行,只要有一边拒绝,流量就会被挡住。
speaker 1: 这听起来会不会容易配置冲突,查起问题来比较麻烦?
speaker 2: 确实是一个需要注意的点,如果两套规则都在用,排查连通性问题的时候要两边都检查一遍。
speaker 1: 那有没有简化的做法?
speaker 2: 有,很多团队会选择只用NSG、把安全列表都放开成最宽松的规则,这样心智负担上就跟AWS安全组几乎一样了,只用一套规则去思考。
speaker 1: 这个建议不错,等于是把复杂度降回我熟悉的模型。
speaker 2: 对,前提是你要清楚这是"选择只用一种机制",而不是"OCI只有一种机制",不然遇到别人配置了安全列表的环境会一头雾水。
