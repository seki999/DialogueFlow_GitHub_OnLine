speaker 1: 先看这张图,VCN、子网、路由表、网关,这套结构跟VPC几乎一模一样。
speaker 2: 对,这是OCI跟AWS概念对应度最高的一块,几乎可以直接照搬VPC的设计经验。
speaker 1: 公有子网配Internet Gateway,私有子网配NAT Gateway,这个逻辑也一样吗?
speaker 2: 一样,公有子网需要直接和公网通信就挂Internet Gateway,私有子网只需要单向访问公网(比如下载软件包)就挂NAT Gateway。
speaker 1: 那路由表的配置思路呢?
speaker 2: 也是同一套逻辑:给子网关联路由表,路由表里定义"去哪个网段走哪个网关",跟VPC的路由表配置方式一致。
speaker 1: 那前面讲的计算实例、Load Balancer这些,都是部署在VCN里的子网里吗?
speaker 2: 对,你可以把VCN想象成一个大房子,子网是房子里的房间,计算实例、负载均衡器都住在某个房间里,数据库也可以配置成只能通过私有子网访问。
speaker 1: 那安全控制这块,跟AWS的安全组是不是也完全一样?
speaker 2: 这里有一点细节差异,OCI有两种机制:安全列表和网络安全组,这个我们下一章专门拆开讲。
