speaker 1: 我已经用了好几年 AWS 了，为什么还要专门学一遍 OCI，不能直接类比着用吗？
speaker 2: 可以类比，但前提是你得先知道哪个对应哪个，不然类比就是瞎猜。
speaker 1: 也是，毕竟服务名字都不一样，比如 OCI 都不叫 EC2。
speaker 2: 对，而且不只是名字不同，有些概念结构也不一样，比如计费单位、账号隔离方式都有差异。
speaker 1: 那这套课程是从头讲云计算基础吗？
speaker 2: 不是，我们默认你已经懂 AWS 那套逻辑，所以每一章都会直接说"OCI 的这个服务对应 AWS 的哪个"。
speaker 1: 明白了，等于是给我一张翻译对照表。
speaker 2: 差不多，我们会覆盖计算、存储、网络、数据库、安全五大类，一共 15 个 OCI 常用服务。
speaker 1: 听起来正好覆盖了我平时在 AWS 上最常用的那些能力。
speaker 2: 对，学完之后你应该能看着 OCI 控制台，脑子里自动浮现出对应的 AWS 服务名。
speaker 1: 那我们从哪里开始？
speaker 2: 先搞清楚 OCI 这家公司的云到底是怎么定位的，再往下拆细节。
