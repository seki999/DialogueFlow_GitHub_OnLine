speaker 1: Object Storage 用法上跟 S3 有区别吗?
speaker 2: 用法上很像,都是"桶(Bucket)+对象(Object)"的模型,支持标准层和归档层。
speaker 1: 那我平时用S3做静态网站托管、存日志这些场景,Object Storage也能直接对应用吗?
speaker 2: 可以,这些场景基本是通用的,只是API调用方式和SDK不同。
speaker 1: 前面提到的出站流量便宜,具体体现在哪?
speaker 2: 就是当你的应用或者用户从Object Storage下载数据到公网的时候,收费标准比S3低,这个差异在大流量下载场景里会被放大。
speaker 1: 举个例子,比如我做一个视频网站,用户经常要下载或回源。
speaker 2: 这种场景下,出站流量费用可能占总成本的很大一部分,这时候把存储放OCI、计算放OCI或者混合部署,都可能带来明显的成本节省。
speaker 1: 那如果我的数据不常访问,是不是也应该像用Glacier一样放到Archive层?
speaker 2: 对,逻辑完全一样,冷数据放归档层能省下不少标准层的存储费用,只是取出来的时候要接受一定的延迟。
