speaker 1: Archive Storage 就是OCI版的Glacier对吧?
speaker 2: 对,定位完全一样,用检索延迟换取更低的长期存储成本。
speaker 1: 检索延迟具体是什么体验,是不是跟Glacier一样不能立刻拿到数据?
speaker 2: 是的,你没法像标准层那样即时下载,需要先发起一个检索请求,等一段时间(具体时长看服务等级)之后数据才变得可访问。
speaker 1: 那什么样的数据适合放进去?
speaker 2: 典型是合规要求必须留存但几乎不会再访问的历史数据,比如几年前的日志、财务留档、旧版本备份。
speaker 1: 如果我误判了,把经常要用的数据放进Archive层会有什么问题?
speaker 2: 会很麻烦,因为每次要用都得先发起检索、等待,体验很差,而且频繁检索反而可能比放标准层还贵,所以分层策略要提前规划好。
speaker 1: 那这四个存储服务(Object、Block、File、Archive)整体上是不是可以看成一套完整的冷热分层体系?
speaker 2: 对,可以这么理解:标准层处理热数据,归档层处理冷数据,块存储和文件存储处理需要挂载访问的场景,四个服务组合起来覆盖了绝大多数存储需求。
