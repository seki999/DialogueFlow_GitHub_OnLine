speaker 1: 那先问个最基础的,OCI 到底是什么,跟 AWS 是同一类东西吗?
speaker 2: 是的,都属于公有云基础设施平台,提供计算、存储、网络这些能力,只不过厂商是 Oracle。
speaker 1: Oracle 不是做数据库的吗,怎么也做云?
speaker 2: 正因为它数据库起家,所以 OCI 在数据库服务上投入特别深,这是它跟 AWS 相比的一个差异化方向。
speaker 1: 那规模上跟 AWS 比怎么样?
speaker 2: 官方说法是全球50个区域,每个区域都提供一致的200多项服务,商业区域和政府区域服务和定价是统一的。
speaker 1: AWS 有些服务在不同区域是不一样的,这点听起来 OCI 更统一?
speaker 2: 对,这是 OCI 官方经常强调的一个卖点,叫"一致性"。
speaker 1: 那成本方面呢,经常听说 OCI 便宜。
speaker 2: 官方给出的对比数字是计算成本低50%,块存储低70%,出站网络流量低80%,不过这是官方口径,具体要看你自己的实例规格。
speaker 1: 网络出站便宜这么多,是不是意味着如果我数据要往外传很多,OCI 更划算?
speaker 2: 对,这也是很多做视频、CDN 回源或者大数据导出场景的团队会拿 OCI 和 AWS 对比的原因。
speaker 1: 那接下来是不是该看看 OCI 整体分了哪几大块服务?
speaker 2: 对,下一章我们就把这200多项服务按照五大类拆开看。
